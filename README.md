# Freedom Family Electric — Quoting App

A standalone web quoting application for **Freedom Family Electric**, replacing an older Google Sheets + Apps Script quote system. Built for the owner to create, review, save, and eventually export electrical quotes for residential work (new builds, custom/spec homes, large homes, service and remodel).

This README is the long-term context file for the project. It is meant for both the owner and any AI coding assistant picking the project up later. Keep the **Recent work** section updated as things change.

## Stack

- Next.js 14 (App Router) + TypeScript
- Tailwind CSS
- Supabase (Postgres) for saved quote storage
- Vercel for deployment (auto-deploys from `main` on GitHub)

## Routes

| Route | Purpose |
| --- | --- |
| `/` | Overview dashboard — a landing page, not an analytics view. Start New Quote button plus an unsaved-working-copy resume card, then one clickable tile per nav-menu location (Work: Quotes / Customers / Schedule / Projects, Money: Receivables / P&L / Email Log, Setup: Pricing Admin) with a live stat where it's cheap (active quotes, customer count, this week's crew jobs, jobs in progress, outstanding $). The most recent quotes (8) sit below with lifecycle badges, linking into each quote. |
| `/quotes` | The quoting tool. Full five-stage lifecycle pipeline (Draft, Prepared, Client Accepted, Pending Payments, Paid in Full) reading saved quotes from Supabase. A New builds / Service calls / All toggle (`?type=`) partitions the two quote types; service calls show a simpler 4-stage strip (Quote / Accepted / Scheduled / Paid). |
| `/customers` | Customer repository. Every quote's Builder / Customer is backed by a re-usable customer record here (backfilled from existing quotes; created on the fly from the quote builder). One row per customer with linked-quote count and total quoted dollars. |
| `/customers/[id]` | Customer detail. Inline editor for name, the email list (add/remove, optional label per email for a husband/wife team), phone, and note, plus the customer's quotes (status + total, linking to `/quotes/[id]`). Editing a customer updates future quotes' autofill only; existing quotes keep their own name/email snapshot. v1 is edit-only (no delete / merge). |
| `/quotes/new` | Quote type chooser. No `?type` → two cards: **New Build** (the catalog-driven quote, `?type=new_build`) or **Service Call / Other** (freeform line items, `?type=service_call`). Resumes the active browser working copy if one exists (routing to the correct builder per stored type). |
| `/quotes/review` | Review the completed quote before saving. Reads the active working copy from browser storage. |
| `/quotes/[id]` | View a saved quote loaded from Supabase by database id. Does not use browser storage. |
| `/quotes/[id]/edit` | Edit a saved quote. Loads it from Supabase into the builder; saving updates the existing row. |
| `/quotes/[id]/print` | Printable Detailed Quote page (customer-facing). One-click **Download PDF** (rendered server-side with react-pdf); the on-screen layout is a preview of the PDF. |
| `/quotes/[id]/print/pdf` | Server route: renders the Detailed Quote to a PDF buffer (react-pdf) and streams it back as a file download. |
| `/quotes/[id]/summary` | Printable Summary Quote page (customer-facing). Condensed: one subtotal per pricing category plus the quote total, no unit prices. One-click Download PDF; on-screen layout is a preview. |
| `/quotes/[id]/summary/pdf` | Server route: renders the Summary Quote to a PDF buffer and streams it back as a file download. |
| `/quotes/[id]/invoices` | Invoicing page for an accepted quote. Set contract amount, rough-in/finish split, and permit fee; mark invoices paid; download invoice PDFs. |
| `/quotes/[id]/invoices/[kind]/print` | Printable invoice (`kind` = `initial`, `finish`, or `service`). Dispatch is by quote type: a service call routes all three kinds through the service invoice doc (a split service call's `initial`/`finish` are its Deposit/Final); a new build routes `initial`/`finish` through the new-build invoice doc. One-click Download PDF; on-screen layout is a preview. Service invoices use a freeform Description / Qty / Amount table. |
| `/quotes/[id]/invoices/[kind]/pdf` | Server route: renders the invoice (`initial`, `finish`, or `service`) to a PDF buffer and streams it back as a file download. |
| `/quotes/[id]/pnl` | **Internal** Job P&L + cost estimator for one quote. Shows revenue (Contracted / Invoiced / Paid, toggle) vs the job's entered cost (material $ buckets + per-person labor split rough-in / finish) and the margin. Costs are entered and saved to `quotes.cost_estimate_data`. Never customer-facing. |
| `/api/email-pdf` | POST API route. Renders a quote/invoice PDF to a buffer (same path as the Download PDF routes) and sends it to a customer as an attachment via Resend. Body: `{ id, doc, invoiceKind?, to, subject?, message? }`. The recipient, subject, and message are pre-filled from the email on file + per-doc defaults and editable before sending. See **Sending email from the app** below. Admin-gated (Phase B). |
| `/api/create-checkout-session` | POST API route. Public. Body: `{ token }` (the HMAC-signed pay-link token). Re-reads the invoice + amount from the DB (service-role; never trusts the browser), then returns a Stripe Checkout URL to redirect to once Stripe is wired (step 3). Until then returns `{ ok: false, configured: false }` so the customer-facing button shows an honest "being set up" message. See **Customer online payment (Stripe)** below. |
| `/api/stripe-webhook` | POST API route. Public (Stripe has no session). Stripe calls it to confirm a payment; security is the Stripe signature header + idempotent handling. Skeleton only until Stripe is wired (step 3); returns 503 until `STRIPE_WEBHOOK_SECRET` is set. See **Customer online payment (Stripe)** below. |
| `/api/keepalive` | GET health-check. Runs one trivial Supabase query (head-count on `quotes`) and returns 200, or 503 on query failure. Hit daily by the `.github/workflows/keepalive.yml` cron so the Supabase free-tier project is not paused after 7 days of inactivity. Public and unauthenticated (counts rows only; reads/writes nothing sensitive). |
| `/pay/[token]` | Public customer payment page. The signed link in an invoice email lands here. Shows the invoice, amount due, and a Pay button (which posts the token to `/api/create-checkout-session`). Reads the invoice from the DB via the service-role client; the amount shown/charged always comes from our DB, never the token. Until Stripe is wired the Pay button shows "online payment is being set up." |
| `/pay/success` | Stripe success redirect. Thanks the customer. The signed webhook (not this URL) is the source of truth for confirming payment. |
| `/pay/canceled` | Stripe cancel redirect. Acknowledges that no payment was made; the invoice stays unpaid. |
| `/login` | Email + password sign-in (Supabase Auth). Public. Unauthenticated users are redirected here by middleware (Phase B); visiting it while signed in redirects to `/`. |
| `/pricing-admin` | Pricing admin. Edit line items, pricing levels, contingencies, project types, crew, and business info/quote notes/invoice terms stored in Supabase. Deactivate-only (no hard delete). |
| `/schedule` | Schedule. Owner-only week view of the crew's jobs: phone-first day list (7-column grid on desktop), Prev/Today/Next week nav, tap a day to add, tap a card to edit. On desktop, drag a card onto another day column to reschedule it (native HTML5; tap-to-edit is the fallback and the only path on mobile). Entries are either a quote job (rough-in/finish for an accepted quote, title/location auto-filled from the quote) or a free-form service call, and can carry one or more crew. Optional clock times, default all-day. Times + a soft overlap warning. Employee self-service access is deferred to the auth pass. |
| `/email-log` | Email audit log. Read-only, recent-first list of every quote/invoice emailed from the app (date, document, recipient, status), including failed send attempts. One row per send. Pairs with the per-quote Email History section on `/quotes/[id]`. |
| `/receivables` | Accounts Receivable. Two stacked tables — Pending Payments (outstanding balances) and Historical Paid (paid in full) — one row per job with rough-in and finish invoice columns. Preset period filter + sort. Read-only, derived from `quotes.invoice_data` + the `email_log` (a finish invoice that has never been emailed is "scheduled", not owed yet, and shown but excluded from the invoiced/outstanding totals until it is emailed or paid). Service-call rows show a single "Service" invoice cell (Finish = N/A) and a "Service call" tag — unless the service call is split into a Deposit + Final, in which case both columns fill (Deposit / Final captions) with the "Service call" tag. The service invoice follows the finish rule (owed only when emailed or paid, not on setup); a split deposit follows the rough-in rule (receivable from setup). |
| `/projects` | Project Status Tracker — every accepted job in ONE combined list (newest first), not separated by quote type; service-call cards carry a "Service call" tag. New builds show an 8-stage strip (Quote, Rough-in, Rough-in Inspection, Rough-in Billed, Finish, Final Inspection, Final Billed, Paid); service calls show a simpler 5-stage strip (Quote / Accepted / Scheduled / Billed / Paid). New builds: the 4 field stages advance by manual buttons (or back-date/clear via an Edit stages panel). Service calls: a Mark-scheduled advance button plus an Edit stages panel (Accepted ↔ Scheduled; Billed/Paid stay derived from invoicing + the `email_log` + `isPaidInFull` so the tracker can never disagree with `/quotes` or `/receivables`). Filter chips (All / In the field / Billing & payment / Completed) via `?filter=`. Each card also shows the crew from the job's schedule assignments. Writes only `quotes.project_status` (new builds) or `quotes.status` (service calls). |
| `/pnl` | **Internal** P&L report. Every job's revenue vs cost grouped by quarter/year, with period subtotals and an overall total. Revenue basis toggle (Contracted / Invoiced / Paid) + period filter. Read-only, derived from existing quote/invoice data + `quotes.cost_estimate_data`. Never customer-facing. |

## File structure

```
middleware.ts                   // refreshes the Supabase Auth session + redirects unauthenticated users to /login (Phase B)
app
  globals.css
  layout.tsx
  page.tsx                      // Overview dashboard (hub)
  quotes
    page.tsx                    // Quote lifecycle pipeline (5 stages)
    new/page.tsx                // Quote type chooser (New Build vs Service Call) → routes to the right builder via ?type=
    review/page.tsx             // Review + save
    [id]/page.tsx               // Saved quote view
    [id]/edit/page.tsx          // Saved quote editor
    [id]/print/page.tsx         // Printable Detailed Quote (preview + Download PDF link)
    [id]/print/pdf/route.tsx    // Server route: renders the Detailed Quote to a PDF buffer (react-pdf)
    [id]/summary/page.tsx       // Printable Summary Quote (category subtotals, preview + Download PDF)
    [id]/summary/pdf/route.tsx  // Server route: renders the Summary Quote to a PDF buffer
    [id]/invoices/page.tsx      // Invoicing setup + invoice list
    [id]/invoices/[kind]/print/page.tsx  // Printable invoice (initial/finish/service, preview + Download PDF)
    [id]/invoices/[kind]/pdf/route.tsx   // Server route: renders the invoice to a PDF buffer
  api
    email-pdf/route.tsx            // POST API route: renders a PDF to a buffer and emails it via Resend (admin-gated)
    create-checkout-session/route.ts  // POST: public; mints/verifies a pay-link token, reads amount from DB, returns a Stripe Checkout URL (step 3) or {configured:false}
    stripe-webhook/route.ts       // POST: public; Stripe signature-verified, idempotent; confirms payment + writes the ledger (skeleton until step 3)
    keepalive/route.ts            // GET health-check: trivial Supabase query, hit daily by the keepalive cron
  pay
    [token]/page.tsx            // Public customer payment page (verifies HMAC token, reads invoice via service-role, shows amount + Pay button)
    success/page.tsx           // Stripe success redirect (thanks the customer; webhook is the source of truth)
    canceled/page.tsx          // Stripe cancel redirect (no charge; invoice stays unpaid)
  login/page.tsx               // Email + password sign-in (Supabase Auth)
  email-log/page.tsx           // Read-only global email audit log (recent sends, newest first)
  pricing-admin/page.tsx       // Pricing admin (items, levels, contingencies, project types, crew, settings)
  receivables/page.tsx         // Accounts Receivable (pending vs paid, two tables)
  schedule/page.tsx            // Schedule (owner-only week view; phone day list / desktop 7-col grid)

components
  app-shell.tsx                  // server: header (brand + New Quote + Menu) + main wrapper
  nav-menu.tsx                   // client: slide-out main menu (Menu button + right panel)
  login-form.tsx                 // client: email + password sign-in via Supabase Auth
  logout-button.tsx             // client: sign out + return to /login (header pill or nav-foot full-width)
  dashboard-active-quote.tsx     // slim resume card for unsaved working copies
  dashboard-quote-section.tsx   // one pipeline stage (Draft / Prepared / Client Accepted)
  status-badge.tsx              // draft/prepared/accepted + invoice paid/unpaid badges
  quote-status-button.tsx       // client button that updates quote.status then refreshes
  quote-builder.tsx
  quote-line-item-picker.tsx
  quote-totals-panel.tsx
  delete-quote-button.tsx
  delete-invoices-button.tsx    // clears invoice_data (keep the quote); blocked when a payment exists
  formatted-number-input.tsx    // currency/decimal input used by the builder + invoice setup
  invoice-builder.tsx           // invoice setup form (contract, split, permit) — new builds
  service-quote-builder.tsx     // freeform service-call quote builder (description/qty/amount lines, no pricing levers)
  service-invoice-builder.tsx   // freeform service-call invoice builder (one kind:"service" invoice by default, or an optional deposit/final split reusing kind:"initial"+"finish"; sum of lines)
  service-stage-strip.tsx       // 5-stage progress strip for a service-call project (Quote/Accepted/Scheduled/Billed/Paid)
  service-project-advance-button.tsx  // Mark scheduled / Set up & send invoice / Awaiting payment on a service project card
  invoice-paid-button.tsx       // toggles an invoice paid/unpaid + records/reverses a payments ledger row
  receivables-table.tsx         // AR tables: period/sort + pending vs paid partition
  schedule-board.tsx            // client week board: week nav, day list/grid, add/edit modal
  schedule-assignment-form.tsx  // client add/edit form (Quote job vs Service call, times, overlap warning)
  crew-editor.tsx               // admin editor for the crew list (add/rename/color/active)
  pricing-admin-ui.tsx          // shared Field/buttons/badges for the admin editors
  pricing-item-editor.tsx       // admin editor for pricing_items
  pricing-level-editor.tsx      // admin editor for pricing_levels
  base-rate-editor.tsx          // admin editor for base_rates (per-sqft rate presets)
  contingency-editor.tsx       // admin editor for contingency_options
  project-type-editor.tsx      // admin editor for project_types
  settings-editor.tsx           // admin editor for the single app_settings row
  pdf/
    pdf-action-bar.tsx          // top toolbar on each printable: Back link + Download PDF anchor + Email PDF button (server component)
    email-pdf-button.tsx         // "Email PDF" client button: inline To/Subject/Message panel, posts to /api/email-pdf
    detailed-quote-document.tsx // react-pdf <Document> recreation of the Detailed Quote printable
    summary-quote-document.tsx  // react-pdf <Document> recreation of the Summary Quote printable
    invoice-document.tsx        // react-pdf <Document> recreation of the printable invoice (initial/finish)
    service-quote-document.tsx  // react-pdf <Document> for the service-call quote (Description/Qty/Amount lines)
    service-invoice-document.tsx // react-pdf <Document> for the service-call invoice (Description/Qty/Amount lines)
    pdf-shared.tsx              // shared colors/styles/header/footer/total/notes/list building blocks for the PDFs
  pay/
    pay-button.tsx              // client: posts the signed token to /api/create-checkout-session, redirects to Stripe or shows "being set up"
    copy-pay-link-button.tsx   // client (admin invoicing page): copies the signed pay URL to clipboard + a preview link

lib
  calculations.ts
  currency.ts
  detailed-quote-pdf.ts         // server: loads quote snapshot + settings, builds the Detailed Quote PDF props
  summary-quote-pdf.ts          // server: loads quote snapshot + settings, builds the Summary Quote PDF props
  invoice-pdf.ts                // server: loads quote row + invoice setup + settings, builds the invoice PDF props (new builds)
  service-quote-pdf.ts          // server: loads a service-call quote + builds the service quote PDF props; fetchQuoteType() dispatch helper
  service-invoice-pdf.ts        // server: loads a service-call invoice + builds the service invoice PDF props
  invoice-calculations.ts       // invoice amount math + outstanding balance + lifecycle stage (new build + service)
  pdf-logo.ts                   // server-only: reads /public/ffe-logo.png into a base64 data URI for react-pdf
  pricing.ts                    // server-side reads of the live pricing catalog + settings
  quote-id.ts                   // client: resolveQuoteIdForSave (keep an existing id on edit, or ask for the next atomic daily number for a new quote); uses the browser client
  schedule.ts                   // crew + schedule types, pure helpers, and fetcher cores that take a Supabase client (shared by server + browser wrappers)
  schedule-server.ts            // server schedule data access (getCrew/getScheduleRange/getSchedulableJobs via getSupabaseServer)
  schedule-client.ts            // client schedule reads (board week-navigation via getSupabaseBrowser)
  send-pdf-email.tsx            // server-only: renders a quote/invoice PDF to a buffer (reuses the /pdf helpers) and sends it via Resend; buildEmailDefaults + isValidEmail live here
  email-log.ts                  // server-only: append-only email audit log (logEmailSend + getEmailHistoryForQuote + getRecentEmailLog)
  payments.ts                   // server-only: Stripe webhook ledger helpers (upsertStripePayment/updatePaymentStatus/setInvoicePaid/readInvoiceAmount/recordWebhookEvent; service-role)
  pay-token.ts                  // server-only: HMAC-signed stateless pay-link tokens (createPayToken/verifyPayToken/buildPayUrl + paymentsConfigured)
  supabase-env.ts               // shared lazy env reader for the auth clients (throws at call time, not build time)
  supabase-browser.ts           // client-only singleton Supabase client (carries the logged-in session)
  supabase-server.ts            // server Supabase client (reads session from cookies; setAll is a no-op in RSC)
  supabase-admin.ts             // server-only service-role client (bypasses RLS; keepalive + Stripe webhook + public /pay page read; never reaches the browser)
  auth.ts                       // server role helpers: getServerUser / isLoggedIn / isAdmin (role from app_metadata)
  quote-storage.ts
  types.ts

public
  ffe-logo.png
```

## How the quote flow works

1. **Dashboard** (`/`) — the overview hub. Owner starts a new quote, jumps to receivables or pricing, or picks a recent quote. The full quote list lives at **`/quotes`** (the quoting tool / lifecycle pipeline), where the owner also opens saved quotes from history.
2. **Build** (`/quotes/new`) — enter project and client info, pricing setup, adders, internal notes. The builder keeps a temporary working copy in the browser (`localStorage` key `ffe-active-quote`) so work is not lost when moving between builder and review. This working copy is **cleared once the quote is saved to Supabase**; after that the owner works from the saved record. The active quote is purely a pre-save working copy. To abandon an unsaved working copy, use the builder's **Reset Quote** button.
3. **Complete Quote** — validates required fields, saves the working copy, routes to `/quotes/review`.
4. **Review** (`/quotes/review`) — review the customer-facing summary and final total, then save.
5. **Save Quote** — writes to Supabase. If editing a saved quote, it **updates** the existing row; otherwise it **inserts** a new row and remembers the id (in memory) so the View/Edit links work. The button locks after a successful save. **After saving, the browser working copy (active quote) is cleared**, so the owner works from the saved file going forward instead of the temporary form. The review page still shows the success message and links from in-memory state.
6. **Saved quote** (`/quotes/[id]`) — view, edit, or delete the saved quote. From here on, the owner works from the saved file, not the active quote.

### Internal notes (owner only)
The builder has an "Internal Notes" text box. These notes are saved with the quote and shown on the owner views (review and saved quote pages), clearly labeled as not shown to the customer. They are **not** included in customer-facing output, including the customer-facing PDFs (Detailed Quote, Summary Quote, and invoices), which load only the safe fields from the saved snapshot.

### Deleting quotes
The saved quote page has a Delete Quote button with a confirm step. It removes the row from Supabase. This requires a delete RLS policy (see Supabase setup below).

### Quote lifecycle pipeline
Every saved quote has a row-level `status` of `draft`, `prepared`, or `accepted`. The `/quotes` pipeline groups saved quotes into five stacked stages that tell the whole customer life cycle: **Draft** (still being worked on), **Prepared** (ready to share with the client, or edit before sending), **Client Accepted** (billing and invoicing start here), **Pending Payments** (invoices set up, money still outstanding), and **Paid in Full** (every invoice paid). The overview dashboard (`/`) rolls these up into summary tiles (active quotes, awaiting payment, paid in full) with quick links into each tool.

- **Save as draft** (in the builder) writes a `draft` row to Supabase with only a client name required, so work-in-progress is saved cross-device instead of only in the browser. The browser working copy is cleared once the draft is saved.
- **Complete Quote** then **Prepare** (on the review page) writes/updates the row as `prepared` and is the customer-ready state.
- **Mark accepted** moves a prepared quote to `accepted`. An accepted quote with no invoices yet shows in **Client Accepted**.
- Once invoices are set up on an accepted quote, it automatically moves to **Pending Payments** (no button to click).
- When every invoice on the quote is marked paid, it automatically moves to **Paid in Full** (no button to click).

The last two stages are not stored on the row. They are derived on the fly from the row status plus `invoice_data` by `lifecycleStage()` in `lib/invoice-calculations.ts`: accepted + no invoice setup = Client Accepted; accepted + invoices with an outstanding balance = Pending Payments; accepted + all invoices paid = Paid in Full. Because it is derived, the pipeline and dashboard always reflect the real payment state and can never disagree with the invoice records.

Status changes (draft/prepared/accepted) are made by `QuoteStatusButton`, a small client component that runs `supabase.update({status})` then `router.refresh()`. Because the pipeline query lives in the server component `app/quotes/page.tsx` (and the overview query lives in `app/page.tsx`), the refresh re-runs it and the quote moves between stages without a manual reload. The same pattern powers the status-aware actions on the saved-quote page. `StatusBadge` now shows the derived lifecycle stage (Draft, Prepared, Accepted, Pending Payments, Paid in Full) rather than just the raw status.

**Service-call quotes** (where `quote_type = 'service_call'`) use a simpler 4-stage lifecycle — Quote / Accepted / Scheduled / Paid — derived by `serviceLifecycleStage()` in `lib/invoice-calculations.ts`: draft+prepared collapse to "Quote"; "Accepted" is the stored `accepted` status; "Scheduled" is a manual **Mark scheduled** button (a stored `status = 'scheduled'` write — new builds never set this); "Paid" is derived from the single service invoice being paid in full. The `/quotes` pipeline shows a New builds / Service calls / All toggle (`?type=`); service calls show four sections (Quote / Accepted / Scheduled / Paid) instead of the five new-build sections. `QuoteStatus` is `draft | prepared | accepted | scheduled` — there is no "paid" status (paid is always derived from invoices, never stored). `StatusBadge` renders both stage types; the `/projects` tracker gives service calls their own 5-stage strip (see `/projects` route above).

**One-time SQL migration (run in the Supabase SQL Editor before deploying this change):**
```sql
update quotes set status = 'prepared' where status = 'completed';
```
This moves every previously saved quote (all stamped `completed`) into the Prepared section so nothing is orphaned.

### Invoicing (accepted quotes)
When a quote is Client Accepted, the saved-quote page and the `/quotes` Accepted card show an **Invoicing** link to `/quotes/[id]/invoices`. There the owner sets up invoicing:

- **Contract amount** defaults to the accepted quote total and is editable.
- **Rough-in / finish split** defaults to 50/50 and is editable. A warning appears if the two percentages do not total 100%.
- **Permit fee** is entered as a dollar amount and shown as its own line on the initial invoice.
- Two invoices are generated: the **initial invoice** (rough-in amount + permit fee) and the **finish invoice** (the remainder).
- Each invoice can be marked **paid / unpaid**. The `/quotes` Accepted card, the overview "Awaiting payment" tile, and the invoicing page show the outstanding balance or "paid in full."
- Each invoice has a printable page (`/quotes/[id]/invoices/[kind]/print`, kind = `initial` or `finish`) with one-click Download PDF; the on-screen layout is a preview of the PDF. Invoice references are `{quote_id}-R` (initial/rough-in) and `{quote_id}-F` (finish).
- **Editable line items (independent of the quote).** The invoice setup form has a **Line Items** editor: an editable table with the same columns as the saved-quote page's customer-facing line items — **Item · Qty · Unit · Unit Price · Line Total** — with the contract shown in a table footer ("Contract (sum of line items)"). Each line is a real priced item pulled from the pricing catalog (the same adder options the quote builder uses), with a **quantity**, an **editable unit price** (defaults to catalog base × the quote's pricing-level multiplier), and a customer-facing **comment** that sits in its own full-width row beneath the line's columns so it spans the whole table. Add lines from a catalog dropdown; edit quantity / price / comment; remove. The **contract amount is the sum of (quantity × unit price)** across the lines — it is no longer hand-entered when line items exist. The rough-in/finish split and permit fee still produce the two invoices (initial = rough-in % of contract + permit; finish = remainder), so the % model is preserved; the line items just become the source of the contract. The line items print in a Scope of Work section on both invoices (Initial + Finish, on screen + PDF) showing each line's "qty × unit price = total" and comment. The list is **seeded from the quote** (its line items, quantities, client prices, and comments) the first time invoicing is set up, then lives on the invoice in `invoice_data.scopeLines` and is edited there independently of the quote — the quote stays a point-in-time estimate and final adjustments happen on the invoice. Legacy invoices set up before line items (which have a hand-entered contract and no `scopeLines`) keep working as-is: they show the editable Contract Amount field and no lines, and the PDF backfills a display-only scope from the quote until the owner adds lines and saves (at which point the contract becomes the sum of the lines). Editing a comment on a paid invoice is money-free and never resets anything; changing a line's quantity/price (and thus the contract) can reset a paid invoice — see below.

Invoice setup is stored as JSONB in the `quotes.invoice_data` column (null until set up). Saving invoice setup preserves existing paid statuses by invoice kind — **except** that if the owner's current line items / split / permit inputs would change the amount of an already-paid **finish** invoice, saving resets that invoice to unpaid (with a clear warning beforehand) so the owner re-marks it paid at the new amount; a paid invoice's amount is never changed silently. The **paid rough-in (initial) invoice is frozen**: once it is marked paid, its amount never changes on save, and any later edit to the line items, contract, or permit fee flows **only to the finish invoice** (finish = contract + permit − paid rough-in). The rough-in/finish split is bypassed while the rough-in is paid, so the % fields are locked and the live preview shows the lock. This matches how FFE actually bills: the rough-in is collected during construction and the finish is settled at the end, so changes after rough-in payment adjust only the remaining finish invoice. `invoice_data.scopeLines` is optional so invoices set up before the field existed still load: the PDF/print path backfills a display-only scope from the quote's `calculation_data.clientFacingLines` when `scopeLines` is absent, and the first time the owner saves with line items the builder persists `scopeLines` so the invoice becomes independent from that save on. Invoicing math lives in `lib/invoice-calculations.ts`.

**Service-call invoicing** (where `invoice_data.quoteType === 'service_call'`) is intentionally simpler in its default shape: a **single** `kind: "service"` invoice with a freeform line editor (Description · Qty · Amount + optional comment, seeded from the quote's `serviceLines` then independent), no rough-in/finish split, no permit, no scope-of-work section, no paid-rough-in freeze — the quick-job model (troubleshoot, a small repair) and the shape every service quote saved before the split option existed keeps. The invoice amount is the sum of the line amounts; the contract amount equals the invoice amount. A paid service invoice whose amount changes resets to unpaid with a warning (same rule as a paid finish). The invoice reference suffix is `-S` (e.g. `Q-20260825-001-S`) and it reserves a shared `INV-NNNN` number on first save. AR owes a service invoice only when it has been emailed or paid (the finish rule, not the rough-in "owed on setup" rule). The service invoice PDF (`components/pdf/service-invoice-document.tsx`) reuses the shared PDF blocks with a Description / Qty / Amount line table (no unit price).

**Optional deposit/final split for a service call.** A service call can instead be billed as a **Deposit** (`kind: "initial"`) + a **Final** (`kind: "finish"`) — reusing the existing new-build two-invoice machinery, with `permitFeeCents = 0` and a deposit % of the contract (default 50; final = remainder). This lets Chad bill a small remodel sized like a service call as 50% up front / 50% at finish without a full new-build quote. The split is **optional** (off by default; quick jobs stay one invoice due on completion) and is chosen by a "Billing schedule" toggle in `ServiceInvoiceBuilder`; the toggle is **locked while any invoice is paid** so a shape change can't orphan a paid record. Once the **deposit is paid it is frozen** — later line-item edits flow only to the final (finish = contract − paid deposit), mirroring the new-build paid-rough-in freeze; the deposit-% field is disabled while the deposit is paid. Detection helper `isUnsplitServiceCall(data)` (= `quoteType === "service_call"` AND a `kind: "service"` invoice is present) narrows the `computeInvoiceAmounts` early-return so a **split** service call (initial + finish, no `service` invoice) falls through to the existing two-invoice split path with the paid-deposit freeze — no new math. The split-service PDFs reuse `ServiceInvoicePdfDocument`; the **final** carries a "previously invoiced (Deposit)" block above the amount due (mirroring the new-build finish). The print/pdf/email dispatch is by **quoteType** (a service call routes ALL of its invoice kinds — service/initial/finish — through the service loaders/documents; a new build routes initial/finish through the new-build ones). The invoicing page (`/quotes/[id]/invoices`) branches: an **unsplit** service call → `ServiceInvoiceBuilder` + one `InvoiceCard`; a **split** service call → `ServiceInvoiceBuilder` + two `InvoiceCard`s (Deposit / Final); a new build → the existing `InvoiceBuilder` + two cards. AR (`/receivables`) renders a split-service row with Deposit + Final cells (both columns) and a "Service call" tag; an unsplit service row keeps the single "Service" cell with Finish = N/A. The `/projects` service-call 5-stage strip lights "Billed" when ANY invoice is emailed/paid and "Paid" when the job is paid in full, so both shapes share one path. The public pay page relabels the title to "Deposit Invoice" / "Final Invoice" / "Service Invoice" for a service call. Stripe/pay, customers, and P&L are already kind-agnostic and need no change. No schema change, no SQL.

**One-time SQL (run in the Supabase SQL Editor before deploying invoicing):**
```sql
alter table public.quotes add column if not exists invoice_data jsonb;
```
No new RLS policies are needed; the existing anon select/update policies cover the column.

### Payments ledger
The per-invoice paid flag in `quotes.invoice_data` is the UI source of truth for outstanding balances and the pipeline stages (it already powers AR, the dashboard, and the badges). A new append-style **`payments`** table is the real ledger of money received — an audit trail that survives a flag flip and lets us later derive "paid" from actual money in instead of a toggle.

Today only the manual **Mark Paid** button writes to it: when the admin marks an invoice paid, the button flips the flag (primary, source of truth) and then best-effort inserts a `payments` row (`method = 'manual'`, `status = 'succeeded'`, `recorded_by` = the admin's email, `paid_at` = now); marking unpaid reverses it by deleting the matching manual row(s) for that invoice (Stripe card/ACH rows are never touched by the manual toggle). The two writes are non-atomic across two queries (a known v1 limitation; a future Postgres RPC can do both in one transaction) — if the ledger write fails the flag still flips and a warning shows so the owner can retry. When Stripe is wired (next step), the webhook will insert `method = 'card'` / `'ach_debit'` rows via the service-role client (bypasses RLS), and a future pass can derive paid state from the ledger instead of the flag.

**One-time SQL (run in the Supabase SQL Editor before deploying):** `supabase/payments-table.sql` — creates the table + indexes + admin-only RLS policies (select/insert/update/delete, gated by `public.is_admin()`). Safe to re-run. The Stripe webhook writes via the service-role client which bypasses RLS, so it needs no policy here.
```sql
create table if not exists public.payments (
  id uuid primary key default gen_random_uuid(),
  quote_id uuid references public.quotes(id) on delete cascade,
  invoice_kind text not null check (invoice_kind in ('initial','finish')),
  amount_cents integer not null check (amount_cents >= 0),
  method text not null check (method in ('card','ach_debit','manual')),
  status text not null default 'succeeded' check (status in ('pending','processing','succeeded','failed','refunded')),
  stripe_payment_intent_id text,
  stripe_charge_id text,
  stripe_session_id text,
  recorded_by text,
  paid_at timestamptz,
  created_at timestamptz not null default now()
);
-- + indexes on quote_id and stripe_payment_intent_id; RLS enabled; admin-only
--   select/insert/update/delete policies gated by public.is_admin().
```

### Customer online payment (Stripe)

Customers can pay an invoice online through a signed link. The link is a **stateless HMAC token** (`lib/pay-token.ts`): it encodes the quote's uuid + invoice kind, signed with `PAY_LINK_SECRET` (a server-only random string). It is unguessable, carries no enumerable DB id, and never expires on its own (rotating `PAY_LINK_SECRET` invalidates every outstanding link at once). The public `/pay/[token]` page verifies the signature, then **re-reads the invoice + amount from the database via the service-role client** — the amount shown and charged always comes from our DB, never from the token or the browser. The page has no user session and the database is admin-only under RLS, so the service-role client (which bypasses RLS) is the only way a public page can read the invoice.

Flow: a Pay button on `/pay/[token]` posts the token to `/api/create-checkout-session`, which re-reads the amount from the DB and returns a Stripe Checkout URL to redirect to. Stripe redirects back to `/pay/success` or `/pay/canceled`, but the **signed webhook** (`/api/stripe-webhook`) is the source of truth for confirming payment — the redirect URL can be tampered with, so the webhook is what writes the `payments` ledger row and flips the invoice's paid flag. The webhook is signature-verified and idempotent (a `stripe_webhook_events` log keyed by Stripe event id, and a unique index on `payments.stripe_payment_intent_id` so multiple events for one payment update one row). Card payments succeed immediately at `checkout.session.completed` (`payment_status = paid`); ACH (US bank account) payments are `processing` at that point and settle days later — `payment_intent.succeeded` flips the flag then. `payment_intent.payment_failed` records a failure (invoice stays unpaid); `payment_intent.canceled` marks the row failed (an owner-cancelled incomplete attempt self-cleans instead of sitting `processing` forever — the flag is not touched, since a cancel is not a refund and the invoice may be paid by a different attempt); `charge.refunded` marks the ledger row refunded and reverses the paid flag.

**Double-payment guard:** before creating a new Stripe session, `/api/create-checkout-session` checks the ledger for an active payment on that invoice (`processing`/`pending`/`succeeded`) and refuses with a clear "a payment is already in progress" message (HTTP 409) if one exists. This closes the live-ACH window where a payment sits `processing` for 1-3 business days while the invoice flag is still unpaid — without it, a customer who reopens the link could pay again and be double-charged. A `failed`/`refunded`/cancelled payment does not block; those are retries. (`hasActivePayment` in `lib/payments.ts`, service-role read; fails open on a read error, since the invoice paid flag is the primary guard.)

**ACH per-payment cap:** Stripe limits ACH (US bank account) to a per-payment max on the account (currently $20,000; Stripe can raise it with more verification). `ACH_LIMIT_CENTS` in `lib/payments.ts` is the single source of truth. `/api/create-checkout-session` only offers `us_bank_account` when the invoice is at or under the cap (`achAvailableForAmount`); over the cap it offers **card only** so the customer never hits a mid-Checkout ACH rejection. The `/pay` page shows a note explaining the cap and pointing to card or a mailed check when an invoice is over it. Bump the constant if Stripe raises the limit.

**Two entry points to a pay link:**
- **Invoice email body:** `buildEmailDefaults` (in `lib/send-pdf-email.tsx`) appends a "Pay online here: <link>" line to the invoice email message, but **only when `paymentsConfigured()` is true** (Stripe wired + `PAY_LINK_SECRET` + `APP_URL` all set). Until then, customers get the standard "payment details are on the invoice" body so they're never sent to a dead "being set up" page.
- **Admin invoicing page:** each invoice card shows a **Copy payment link** button (server-mints the URL, client copies it + a Preview link) whenever `PAY_LINK_SECRET` + `APP_URL` are set.

**Status:** steps 2 + 3 are built. Step 2 laid the plumbing (signed tokens, public `/pay` pages, route skeletons, gated email link, copy-link button) with no Stripe keys. Step 3 wired the real Stripe Checkout Session (card + US bank account / ACH) in `/api/create-checkout-session` and the live signature-verified webhook handler in `/api/stripe-webhook` (`lib/payments.ts` does the idempotent `payments` upsert + invoice-flag flip via the service-role client). No card numbers, CVC, or full bank details are ever stored — Stripe holds them.

**One-time setup to turn on online payment (Stripe, test mode first):**
1. Run `supabase/stripe-webhook-table.sql` in the Supabase SQL Editor (creates `stripe_webhook_events` + a unique index on `payments.stripe_payment_intent_id`; safe to re-run).
2. In Vercel env, set `STRIPE_SECRET_KEY` and `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` (your Stripe **test** keys), and `STRIPE_WEBHOOK_SECRET` (after step 4). Redeploy.
3. In the Stripe dashboard (test mode): **Settings → Payment methods** and enable **US bank account** (ACH) so it appears in Checkout alongside cards.
4. In the Stripe dashboard (test mode): **Developers → Webhooks → Add endpoint** pointing at `https://admin.freedomfamilyelectric.com/api/stripe-webhook`, listening for `checkout.session.completed`, `payment_intent.succeeded`, `payment_intent.processing`, `payment_intent.payment_failed`, `payment_intent.canceled`, and `charge.refunded`. Stripe shows the **signing secret** — set it as `STRIPE_WEBHOOK_SECRET` in Vercel and redeploy.
5. Test end-to-end: from the admin invoicing page, copy a payment link, open it, pay with the test card `4242 4242 4242 4242` (any future date + any CVC) and confirm the invoice flips to paid + a `payments` row appears (`method = card`, `recorded_by = stripe`). Then test an ACH/bank payment: ACH uses **instant bank verification** (Stripe Financial Connections / Plaid), so the customer logs into their bank in Checkout (use Stripe's test bank login credentials from the docs); the manual "enter bank manually" option is intentionally removed because that path can't complete in one Checkout and leaves the payment "incomplete." Confirm the ACH payment lands as `processing` then `succeeded` a moment later and the invoice flips to paid. Watch the endpoint's event log in the Stripe dashboard to confirm events are received 200.
6. Go live: once the owner's Stripe business account + bank accounts are set up, swap the test keys for live ones and re-register the webhook in live mode.

### Accounts Receivable (`/receivables`)
A read-only collections page that shows every accepted quote with invoice setup, split into **two stacked tables**: **Pending Payments** (any job with an outstanding balance on a *billed* invoice, plus jobs whose finish is still scheduled/not yet billed) and **Historical Paid** (jobs paid in full). Each row is one job (customer) with separate **Rough-In** and **Finish** columns showing that invoice's amount, a Paid/Unpaid badge, and the per-invoice outstanding (or the paid date), plus a job-level **Total Outstanding** and the earliest receivable date. Row links go to the existing invoicing page (`/quotes/[id]/invoices`) where invoice PDFs are downloaded or invoices marked paid.

**Receivable model (email-gated finish).** An invoice is "receivable" (billed, counts toward AR) when it is paid, or it is the rough-in (billed at setup), or it has been emailed (a `sent` `email_log` row for that quote + kind). The finish invoice is created at invoice setup but not actually billed until after the sheetrock gap, so before it is emailed (or paid) it is **scheduled** — shown in the Finish column as the amount with a "Scheduled / not yet billed" badge and excluded from the invoiced/outstanding totals. This keeps AR from overstating what is owed right now: a job with a paid rough-in and an unbilled finish shows $0 outstanding and stays in Pending Payments (not Historical Paid), because work is still in progress. The rough-in always counts (it is the current invoice, often handed over in person / collected as cash, not always emailed). The receivable date is the first sent email date, falling back to `paidAt`, then `issuedAt`.

Controls live client-side in `components/receivables-table.tsx`: a preset period filter (All time / This month / Last 30 days / This quarter / This year, scoped by the job's earliest receivable date) and a sort (oldest first / largest outstanding / newest / client). The page (`app/receivables/page.tsx`, `force-dynamic`) queries accepted quotes with `invoice_data` not null, batch-loads the emailed-state via `loadInvoiceReceipts` (one `email_log` query), flattens each into a `ReceivableJob` (reusing `invoiceIsReceivable`, `outstandingCents`, `isPaidInFull`, `receivableInvoicedCents`, `scheduledFinishCents`, `invoiceReference`, plus `invoiceOutstandingCents` for per-invoice balances), and hands them to the table. No new tables or RLS policies. Summary cards at the top show total invoiced, total outstanding (emphasized), and total paid — all counting only receivable invoices.

The same receivable model drives the dashboard's "Awaiting payment" tile + recent-row badges (`app/page.tsx`), the quote pipeline stages (`app/quotes/page.tsx` + `components/dashboard-quote-section.tsx`), and the saved-quote / invoicing page headers ("Outstanding: $X", "Finish pending: $Y", or "Paid in full"). The shared helpers in `lib/invoice-calculations.ts` take an optional `receipts` param; **omitting it preserves the legacy behavior** (every invoice receivable), so `lib/pnl.ts` (the P&L report, which reasons about the full contract) is unchanged.

### Sending email from the app
Each of the four customer-facing printables (Detailed Quote, Summary Quote, Initial Invoice, Finish Invoice) has an **Email PDF** button next to the existing **Download PDF**. It opens a small panel with To / Subject / Message, pre-filled from the customer's email on file (`quote_data.clientEmail`) plus per-doc defaults built by `buildEmailDefaults` in `lib/send-pdf-email.tsx` (no em dashes in the copy). The owner edits any field, then Send posts to `POST /api/email-pdf`.

The endpoint renders the requested PDF to a buffer using the **same** `load*PdfInput` helpers + `renderToBuffer` that the Download PDF routes use, so the emailed attachment is byte-identical to the downloaded one (and matches the on-screen preview). The message body is plain text; the PDF is attached. Internal notes are excluded as on every customer-facing PDF. No new PDF rendering code. The email's **Reply-To** is set to the business email on file (`app_settings.business_email`, e.g. `freedomfamilyelectric@gmail.com`), so customer replies land in the owner's inbox even though the email is sent from the `EMAIL_FROM`/`EMAIL_FROM_INVOICES` address on the verified domain.

Sending goes through **Resend**. One-time setup (owner, before the feature can send to real customers):
1. `npm install` (the `resend` package was added to `package.json`); Vercel installs it automatically on deploy.
2. In Resend, **verify a domain you own** (add the DNS records Resend shows you). Until a domain is verified, Resend only delivers to its own test address (`onboarding@resend.dev`), so the owner can self-test before going live but cannot reach a customer.
3. Add `RESEND_API_KEY` and `EMAIL_FROM` (an address on the verified domain) to Vercel env and `.env.local`. Optionally add `EMAIL_FROM_INVOICES` so invoices send from a separate address; if unset, invoices use `EMAIL_FROM`. Both `EMAIL_FROM` and `EMAIL_FROM_INVOICES` must be on the verified domain (any localpart works once the domain is verified).

**Endpoint is now gated (Phase B).** `/api/email-pdf` requires an admin session — anonymous callers get 401, non-admins 403 (`getServerUser()` in `lib/auth.ts`). The middleware front-door redirect keeps anonymous users off the pages that expose the button. All data access (including the email-log write) now goes through the logged-in session (Phase C2); the legacy wide-open `anon` policies are dropped in Phase C3, after which the database is fully private.

### Email audit trail
Every send through `/api/email-pdf` appends one row to an append-only `email_log` table (in `lib/email-log.ts`, `logEmailSend` is best-effort so a logging failure never breaks a send). The row records the quote, doc type, invoice kind, reference + doc-title snapshots, recipient, subject, the Resend message id, status (`sent`/`failed`), any error, and the timestamp. Both successful and failed sends are logged so the owner can see attempts, not just successes. The `reference`/`doc_title` snapshots + `on delete set null` keep history readable even if the quote row is later deleted.

It surfaces in two places: an **Email History** section on the saved-quote page (`/quotes/[id]`, per-quote, newest first) and a global **Email Log** page (`/email-log`, nav link, 50 most recent sends across all quotes, newest first). Both are read-only; the table is append-only (no update/delete).

**One-time SQL (run in the Supabase SQL Editor before deploying the audit trail):**
```sql
create table if not exists public.email_log (
  id uuid primary key default gen_random_uuid(),
  quote_id uuid references public.quotes(id) on delete set null,
  doc text not null check (doc in ('detailed','summary','invoice')),
  invoice_kind text check (invoice_kind is null or invoice_kind in ('initial','finish')),
  reference text not null default '',
  doc_title text not null default '',
  recipient text not null,
  subject text not null,
  provider_message_id text,
  status text not null default 'sent' check (status in ('sent','failed')),
  error text not null default '',
  sent_at timestamptz not null default now()
);

create index if not exists email_log_quote_idx on public.email_log(quote_id);
create index if not exists email_log_sent_at_idx on public.email_log(sent_at desc);

alter table public.email_log enable row level security;
create policy "Allow browser read email log"  on public.email_log for select to anon using (true);
create policy "Allow browser insert email log" on public.email_log for insert to anon with check (true);
```
No update/delete policies (append-only). These permissive anon policies mirror the rest of the dev posture and **must be tightened** (reads to owner/admin, writes server-side only) alongside the auth/RLS pass.

## Pricing and calculation logic

Pricing and customer-facing text now live in Supabase (not a static file) and are edited through the `/pricing-admin` page. The two builder entry points (`/quotes/new` and `/quotes/[id]/edit`) are async server components that call `getPricingCatalog()` in `lib/pricing.ts` and pass the catalog down into `<QuoteBuilder>`, which threads the items into the picker and the levels/contingencies into `calculateQuote`. The printable Detailed Quote, Summary Quote, and invoice pages call `getSettings()` for business name/email, default quote notes, and invoice payment terms. All of these read live on every request (`force-dynamic` + the shared client's `cache: "no-store"`), so a price change in `/pricing-admin` shows up the next time a quote is started or a printable is opened.

Calculations live in `lib/calculations.ts`. Money is stored in cents; `lib/currency.ts` formats it. The catalog types (`PricingItem`, `PricingLevel`, `ContingencyOption`, `ProjectType`, `AppSettings`, `PricingCatalog`) are in `lib/types.ts`. The built-in base-rate preset fallback ($5-$12, used when the `base_rates` table is empty/missing) is in `lib/base-rates.ts`.

Base rate (evaluated in order):
- Manual mode = manual base rate
- Builder/Spec = $5.00 / sq ft
- Small home under 2,500 sq ft = $7.00 / sq ft
- High Ceiling / Complex Switching = $6.50 / sq ft
- Default = $6.00 / sq ft

Pricing levels and contingency options are multiplier rows in Supabase (edited in `/pricing-admin`). The seeded defaults are: levels Contractor/Builder 0.90, Standard/Custom 1.00, Premium/High-End 1.20; contingencies 0% / 5% / 10% / 15% (1.00 / 1.05 / 1.10 / 1.15). Project types default to Custom Home, Spec Home, New Build, Remodel, Service Work.

Calculation:
```
base package        = square footage * base rate
adders base total   = sum(quantity * base price)
total before client = base package + adders base total
combined multiplier = pricing level multiplier * contingency multiplier
final quote         = total before client * combined multiplier
```

`calculateQuote` resolves the pricing level and contingency by stable id, falling back to an explicit default id (not array index) so reordering or deactivating rows never silently changes a quote. The builder dropdowns show rows where `active` is true **plus** the quote's currently-selected value, so editing an old quote that references a now-inactive level/item still resolves and displays it. Quotes store the project-type **display name** (not the row id) for backward compatibility with existing saved quotes.

Pricing rows are deactivated, never hard-deleted, so old quotes and drafts keep resolving. Saved quotes already store full `quote_data` and `calculation_data` JSONB snapshots, which protects historical pricing: only the live builder re-resolves pricing; saved views and printables always render from the snapshot.

**One-time SQL (run in the Supabase SQL Editor before deploying pricing admin):**
```sql
create table if not exists public.pricing_items (
  id text primary key,
  category text not null,
  name text not null,
  unit_type text not null,
  base_price_cents integer not null default 0,
  active boolean not null default true,
  sort_order integer not null default 0,
  updated_at timestamptz not null default now()
);

create table if not exists public.pricing_levels (
  id text primary key,
  name text not null,
  multiplier numeric not null default 1,
  description text not null default '',
  active boolean not null default true,
  sort_order integer not null default 0,
  updated_at timestamptz not null default now()
);

create table if not exists public.base_rates (
  id text primary key,
  name text not null,
  rate_cents integer not null default 600,
  active boolean not null default true,
  sort_order integer not null default 0,
  updated_at timestamptz not null default now()
);

create table if not exists public.contingency_options (
  id text primary key,
  name text not null,
  multiplier numeric not null default 1,
  active boolean not null default true,
  sort_order integer not null default 0,
  updated_at timestamptz not null default now()
);

create table if not exists public.project_types (
  id text primary key,
  name text not null,
  active boolean not null default true,
  sort_order integer not null default 0,
  updated_at timestamptz not null default now()
);

create table if not exists public.app_settings (
  id integer primary key default 1,
  business_name text not null default '',
  business_email text not null default '',
  business_tagline text not null default '',
  default_quote_notes text not null default '',
  invoice_payment_terms text not null default '',
  -- Internal cost-estimate defaults (default material bucket names + default
  -- hourly labor rate). Added by supabase/cost-estimate-columns.sql. Empty
  -- object by default; the app falls back to built-in defaults (Chad's
  -- buckets + $75/hr, lib/cost-estimate.ts) when empty.
  cost_estimate_defaults jsonb not null default '{}'::jsonb,
  updated_at timestamptz not null default now()
);
```

The `quotes` table also carries a nullable `cost_estimate_data jsonb` column (added by the same `supabase/cost-estimate-columns.sql` migration) holding each job's editable cost estimate (material $ buckets + per-person labor split rough-in / finish). Old quotes have null; the Job P&L page builds a default (the global default buckets at $0 + one blank labor line) on first open and nothing is persisted until the owner saves. Both columns are covered by the existing admin-only policies on `quotes` / `app_settings` (no new RLS). Run `supabase/cost-estimate-columns.sql` once in the Supabase SQL Editor to add both.
Then seed the five tables from the current values (the 41 pricing items, 3 levels, 4 contingencies, 5 project types, and the business info / default quote notes / invoice payment terms that used to live in `lib/seed-data.ts`). Enable RLS. The original build-phase permissive `anon` policies (below) were **dropped in Phase C3** and replaced by `authenticated`/`admin` policies (Phase C1, gated by `public.is_admin()`); they're kept here only as the historical record. Note the policy names embed the specific table name (e.g. `pricing_items`, not a generic `pricing`; `app_settings`, not `settings`) — this is how they actually exist in the database, and dropping by any other name is a silent no-op:
```sql
alter table public.pricing_items       enable row level security;
alter table public.pricing_levels      enable row level security;
alter table public.base_rates          enable row level security;
alter table public.contingency_options enable row level security;
alter table public.project_types       enable row level security;
alter table public.app_settings        enable row level security;

-- Build-phase (SUPERSEDED + dropped in Phase C3; admin policies from C1 now govern these tables.)
create policy "Allow browser insert pricing_items during app build" on public.pricing_items       for insert to anon with check (true);
create policy "Allow browser read pricing_items during app build"   on public.pricing_items       for select to anon using (true);
create policy "Allow browser update pricing_items during app build" on public.pricing_items       for update to anon using (true) with check (true);
create policy "Allow browser insert pricing_levels during app build" on public.pricing_levels      for insert to anon with check (true);
create policy "Allow browser read pricing_levels during app build"   on public.pricing_levels      for select to anon using (true);
create policy "Allow browser update pricing_levels during app build" on public.pricing_levels      for update to anon using (true) with check (true);
create policy "Allow browser insert contingency_options during app build" on public.contingency_options for insert to anon with check (true);
create policy "Allow browser read contingency_options during app build"   on public.contingency_options for select to anon using (true);
create policy "Allow browser update contingency_options during app build" on public.contingency_options for update to anon using (true) with check (true);
create policy "Allow browser insert project_types during app build" on public.project_types       for insert to anon with check (true);
create policy "Allow browser read project_types during app build"   on public.project_types       for select to anon using (true);
create policy "Allow browser update project_types during app build" on public.project_types       for update to anon using (true) with check (true);
create policy "Allow browser insert app_settings during app build" on public.app_settings       for insert to anon with check (true);
create policy "Allow browser read app_settings during app build"  on public.app_settings        for select to anon using (true);
create policy "Allow browser update app_settings during app build" on public.app_settings       for update to anon using (true) with check (true);
```
If the migration is skipped, the builder shows an empty picker plus a "Pricing not configured" banner, and print pages show empty business info. No new policies are needed beyond these dev-permissive anon ones; they must be tightened (and `/pricing-admin` gated to admin) alongside the existing RLS-tightening item before production.

## Supabase setup

Environment variables (set in Vercel, and in `.env.local` for local dev):
```
NEXT_PUBLIC_SUPABASE_URL           // base project URL, e.g. https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY          // server-only service-role key (keepalive + future Stripe webhook + public /pay page reads); never NEXT_PUBLIC_
RESEND_API_KEY                     // Resend API key (server-only, used by /api/email-pdf)
EMAIL_FROM                         // RFC "From" for quotes (server-only), on a Resend-verified domain,
                                   //   e.g. "Freedom Family Electric <quotes@yourdomain.com>"
EMAIL_FROM_INVOICES                // (optional) RFC "From" for invoices, e.g.
                                   //   "Freedom Family Electric <invoices@yourdomain.com>".
                                   //   If unset, invoices fall back to EMAIL_FROM.
APP_URL                            // public origin of the site, e.g. https://admin.freedomfamilyelectric.com (server-only; used to build absolute pay links)
PAY_LINK_SECRET                    // server-only random string (>= 32 chars; `openssl rand -hex 32`). Signs the customer pay-link tokens. Rotating it invalidates all outstanding pay links.
STRIPE_SECRET_KEY                  // (step 3) server-only Stripe secret key; until set, online payment shows "being set up"
STRIPE_WEBHOOK_SECRET              // (step 3) signing secret for the Stripe webhook endpoint (from the dashboard when you register /api/stripe-webhook)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY // (step 3) Stripe publishable key (safe for the browser)
```
The Supabase URL is the base project URL, **not** the REST endpoint. The `RESEND_API_KEY` / `EMAIL_FROM` / `EMAIL_FROM_INVOICES` vars are server-only (no `NEXT_PUBLIC_` prefix) and are only needed once the email feature is enabled. Quotes (Detailed + Summary) send from `EMAIL_FROM`; invoices (Initial + Finish) send from `EMAIL_FROM_INVOICES` when set, otherwise `EMAIL_FROM`. See **Sending email from the app** below.

### One-time SQL migrations (run in the Supabase SQL Editor)

Service-call quote type (run once before deploying the service-call feature):
```sql
alter table public.quotes add column if not exists quote_type text not null default 'new_build';

-- Widen the two invoice_kind check constraints so 'service' is allowed.
alter table public.payments drop constraint if exists payments_invoice_kind_check;
alter table public.payments add constraint payments_invoice_kind_check
  check (invoice_kind in ('initial','finish','service'));

alter table public.email_log drop constraint if exists email_log_invoice_kind_check;
alter table public.email_log add constraint email_log_invoice_kind_check
  check (invoice_kind is null or invoice_kind in ('initial','finish','service'));
```
Verify the actual constraint names before dropping (the README documents both check clauses). No new table, no new RLS — writes go to `quotes` (already admin-only under Phase C). Old quotes default to `new_build` and resolve exactly as before. (See also the earlier one-time SQL blocks: `invoice_data` jsonb column, `payments` table, `project_status` jsonb, `project_name` column, `base_rates` table, `customers` table, `invoice_sequences` + `next_invoice_number()`.)

### Supabase keepalive (prevent free-tier pause)

Supabase pauses a free-tier project after 7 days of no inbound activity. A daily GitHub Actions cron pings `GET /api/keepalive`, which runs one trivial Supabase query so the project counts as active and is not paused. Scheduled workflows run only on the default branch, so this is effective only when committed to `main`.

- The route is `app/api/keepalive/route.ts` (public, unauthenticated, counts rows only). The cron is `.github/workflows/keepalive.yml` (runs ~08:17 UTC daily; also runnable on demand from the Actions tab).
- **No setup required.** The production URL (`https://admin.freedomfamilyelectric.com`) is hardcoded in the workflow, so it runs with zero configuration. If the URL ever changes, update `APP_URL` in `.github/workflows/keepalive.yml`.
- You can see every run (and any failures) under the repo's **Actions** tab → "Supabase keepalive". A failed ping shows a red X; a paused/healthy Supabase project is the first thing to check if it goes red.

### Supabase Auth (owner/admin login) — in progress

Login is being added in four low-risk phases so the live app keeps working between each. Phases A, B, C are **live and complete**: you must sign in to use the app, the email endpoint is gated to admins, all data access goes through the logged-in session (the authenticated server + browser clients), and the legacy wide-open `anon` policies have been dropped — so the **database is fully private** (anonymous direct access is denied; only a logged-in admin can read/write quotes, pricing, invoices, email log, and settings; any logged-in user can read the schedule). Phase D (role-gate confirmation) is a paper review remaining.

**Roles:** `admin` (the owner + business owner — full access) and `team_member` (crew; schedule-only, wired in a later phase). Role is stored in each user's `app_metadata` (set in the Supabase dashboard, signed into the JWT) so it can't be self-edited; `lib/auth.ts` reads it via `getServerUser()`, and the database's RLS reads the same field via a `public.is_admin()` security-definer function (added in C1) so server-side gates and database-level rules always agree. Auth-aware clients (Phase A): `lib/supabase-env.ts` / `lib/supabase-browser.ts` / `lib/supabase-server.ts`; `middleware.ts` refreshes the session on every request and redirects unauthenticated users to `/login`.

- **Phase A (done):** auth infrastructure + login + logout. App stayed fully open; login worked but was optional.
- **Phase B (done):** middleware redirects unauthenticated users to `/login` (except public routes: `/login`, `/pay/*`, `/api/keepalive`, `/api/stripe-webhook`); `POST /api/email-pdf` is gated to an admin session (401/403). The app requires sign-in.
- **Phase C1 (done):** added `authenticated`/`admin` RLS policies alongside the existing `anon` ones (via `public.is_admin()`), and granted `next_quote_id` to authenticated. No code deploy; app kept working on the anon policies. SQL: `supabase/phase-c-1-add-policies.sql`.
- **Phase C2 (done):** switched all data access from the bare anon client to the logged-in session — server pages + the pricing/email-log/PDF libs use `getSupabaseServer()` (per-request, RLS-enforced), client components + `lib/quote-id` use `getSupabaseBrowser()` (session-carrying singleton), and the keepalive route uses a new service-role client `lib/supabase-admin.ts` (no user session; bypasses RLS). `lib/schedule` was split into a shared types/helpers module (`lib/schedule.ts`) plus `lib/schedule-server.ts` and `lib/schedule-client.ts` wrappers, so `cookies()`/`next/headers` never lands in a client bundle. The bare anon client `lib/supabase.ts` was removed. Anon policies still exist, so the app kept working throughout. Requires the `SUPABASE_SERVICE_ROLE_KEY` env var on Vercel.
- **Phase C3 (done):** dropped the legacy `anon` policies so the database is fully private (anonymous direct access is denied), and revoked the anon grant on `next_quote_id`. The first pass (`supabase/phase-c-3-drop-anon-policies.sql`) dropped them by name and missed 14 on the pricing tables + `app_settings` because their real DB names embed the table name and didn't match the README-documented names (a silent no-op); `supabase/phase-c-3b-drop-remaining-anon.sql` finished the job by reading `pg_policies` and dropping every anon policy in the public schema directly. Verified: zero `{anon}` rows in `pg_policies`. Rollback (if ever needed): `supabase/phase-c-rollback-re-add-anon.sql`.
- **Phase D:** confirm role gates (employees excluded from quotes/pricing/receivables/email-log automatically; admin full access incl delete + reopen).

**One-time Supabase setup (Phase A):**
1. Supabase dashboard → **Authentication → Providers → Email**: enable Email, ensure "Confirm email" is off for now (or confirm the users manually), and allow `freedomfamilyelectric@gmail.com` + `glwalke17@gmail.com`.
2. **Authentication → Users → Add user**: create the two admin users (email + a password you choose). Send the password to the business owner out-of-band.
3. Set each user's role: for each user, **Authentication → Users → (user) → ... → set `app_metadata`** to `{ "role": "admin" }` (via the dashboard user panel or a one-time SQL update — see below). Without this, `getServerUser()` defaults to `team_member`.
4. Test: visit `/login`, sign in, land on `/`; a Logout button is in the header.

```sql
-- Set the admin role on the two admin users (run once in the Supabase SQL Editor).
-- Replace the emails if needed.
update auth.users
set raw_app_meta_data = coalesce(raw_app_meta_data, '{}'::jsonb) || '{"role":"admin"}'::jsonb
where email in ('freedomfamilyelectric@gmail.com', 'glwalke17@gmail.com');
```

`quotes` table:
```sql
create table if not exists public.quotes (
  id uuid primary key default gen_random_uuid(),
  quote_id text not null,
  quote_date date not null,
  client_name text not null,
  client_email text,
  project_street text not null,
  project_city text not null,
  project_state text not null,
  project_zip text not null,
  project_type text not null,
  square_footage integer not null,
  base_pricing_mode text not null,
  manual_base_rate_cents integer not null,
  high_ceiling_or_complex_switching boolean not null default false,
  pricing_level_id text not null,
  contingency_id text not null,
  internal_notes text,
  quote_data jsonb not null,
  calculation_data jsonb not null,
  client_quote_total_cents integer not null,
  status text not null default 'draft',
  invoice_data jsonb,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);
```

`quote_id` is unique per quote. The server-side sequence guarantees no two auto-generated IDs collide; the unique constraint is the database-level guard (it predates the read-only field, when the owner could type a custom ID that might clash with a past or future `Q-YYYYMMDD-NNN`). Run once in the Supabase SQL Editor — first confirm there are no existing duplicates, then add the constraint:
```sql
-- 1. Confirm there are no duplicate quote_ids (must return 0 rows):
select quote_id, count(*)
from public.quotes
group by quote_id
having count(*) > 1;

-- 2. Add the unique constraint (only if step 1 returned nothing):
alter table public.quotes
  add constraint quotes_quote_id_key unique (quote_id);
```
If step 1 ever returns rows, resolve the duplicates (edit or delete the wrong row) before adding the constraint, or the `alter table` will fail. Once the constraint exists, a colliding insert/update throws and the app surfaces it as a "Save failed" error (the save paths in `quote-builder.tsx` and `review/page.tsx` already report insert errors).

The quote ID is a daily sequence (`Q-YYYYMMDD-NNN`) assigned at save time via a Postgres function that atomically increments a per-day counter, so two people saving at the same instant can never collide. The builder's Quote ID field is read-only (placeholder "Assigned on save") until the quote is actually saved; manual entry was removed on 2026-08-11, so a new quote always mints a system number. Editing a saved quote keeps its existing ID (the read-only field is prefilled with it and passed back through). The function is a `security definer` function that atomically increments the counter via `INSERT ... ON CONFLICT ... RETURNING` and returns the formatted ID. It bypasses RLS on the counter table, so the table itself has no policies (direct anon access is denied; only the function can touch it). Run once in the Supabase SQL Editor before deploying the sequencing:

Two honesty caveats on "server-side" and "no gaps": the RPC is invoked from the browser using the public anon key (the *increment* is atomic in Postgres, but the call is not an authenticated server action), so until auth/RLS tightening lands anyone with the anon key can call `next_quote_id` and burn arbitrary gaps; and the number is reserved before the quote row is written, so a save that fails after the increment (network error, RLS denial, constraint violation) can also leave a gap. Abandoned builder sessions (closed before Save) do not burn a number. Moving the call behind an authenticated server action is part of the pending hardening pass.
```sql
create table if not exists public.quote_sequences (
  day text primary key,        -- 'YYYYMMDD'
  seq integer not null default 0
);

alter table public.quote_sequences enable row level security;
-- No policies: direct anon access is denied. The security-definer function
-- below is the only path that can read/write the counter.

create or replace function public.next_quote_id(p_day text)
returns text
language plpgsql
security definer
set search_path = public
as $$
declare
  v_day text;
  v_seq integer;
begin
  v_day := p_day;
  insert into public.quote_sequences (day, seq) values (v_day, 1)
    on conflict (day) do update set seq = public.quote_sequences.seq + 1
    returning seq into v_seq;
  return 'Q-' || v_day || '-' || lpad(v_seq::text, 3, '0');
end;
$$;

grant execute on function public.next_quote_id(text) to anon;

-- Defensive default: a raw insert that omits status lands as draft, not the
-- legacy 'completed'. (Every app insert sets status explicitly, so this only
-- guards direct/SQL inserts.)
alter table public.quotes alter column status set default 'draft';
```
The day prefix is the quote's own `quote_date` (passed in as `p_day`), not the database `now()`, so a quote's ID prefix and its `quote_date` always agree even if the owner backdates the quote or saves a tab left open across midnight. The counter is keyed by that day, so two quotes sharing a `quote_date` get sequential numbers.

If you are upgrading from the earlier no-argument `next_quote_id()`, drop the old overload first and grant the new one (the parameter changes the function signature, so the old grant does not carry over):
```sql
drop function if exists public.next_quote_id();
grant execute on function public.next_quote_id(text) to anon;
```

RLS is enabled. Development policies (anon can insert, select, update, delete). These are intentionally permissive for the build phase and **must be tightened before production** (add auth, limit to owner/admin, move writes through server actions if possible). Current dev policies:
```sql
create policy "Allow browser insert quotes during app build" on public.quotes for insert to anon with check (true);
create policy "Allow browser read quotes during app build"   on public.quotes for select to anon using (true);
create policy "Allow browser update quotes during app build" on public.quotes for update to anon using (true) with check (true);
create policy "Allow browser delete quotes during app build" on public.quotes for delete to anon using (true);
```
The **update** policy is required for the status pipeline (`QuoteStatusButton`), saving drafts against an existing row, and editing saved quotes. If status changes fail in testing, verify the update policy exists.

### Scheduling (crew + assignments)

The owner schedules his crew (Adam, Johnathan full-time; Peyton intern) onto rough-in and finish jobs for accepted quotes, plus free-form entries (service calls, warranty visits, supply runs) that have no quote. `crew` is a small editable list (managed under Pricing → Crew; deactivate instead of delete so past schedules keep their label). An assignment can carry **one or more** crew members (so two people can be sent to one job), tracked through the `schedule_assignment_crew` join table — `schedule_assignments` itself holds no crew column. `quote_id` and `phase` are both optional (null = a free-form/service-call entry with no phase), and `title`/`location` are always stored on the assignment as an editable snapshot, so editing them never touches the quote and deleting a quote (`on delete set null`) never wipes schedule history. Only `work_date` is required (plus at least one crew via the join table). Run once in the Supabase SQL Editor:
```sql
create table if not exists public.crew (
  id uuid primary key default gen_random_uuid(),
  name text not null,
  role text not null default 'full_time' check (role in ('full_time','intern')),
  color text not null default '#344236',
  active boolean not null default true,
  sort_order int not null default 0,
  created_at timestamptz not null default now()
);

create table if not exists public.schedule_assignments (
  id uuid primary key default gen_random_uuid(),
  quote_id uuid references public.quotes(id) on delete set null,
  phase text check (phase in ('rough_in','finish')),
  title text not null default '',
  location text not null default '',
  work_date date not null,
  start_time time,
  end_time time,
  notes text not null default '',
  status text not null default 'scheduled' check (status in ('scheduled','completed','cancelled')),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  check (start_time is null or end_time is null or end_time > start_time)
);

-- One row per (assignment, crew). An assignment can have several crew members.
create table if not exists public.schedule_assignment_crew (
  assignment_id uuid not null references public.schedule_assignments(id) on delete cascade,
  crew_id uuid not null references public.crew(id) on delete cascade,
  primary key (assignment_id, crew_id)
);

create index if not exists schedule_assignments_work_date_idx on public.schedule_assignments(work_date);
create index if not exists schedule_assignments_quote_idx on public.schedule_assignments(quote_id);
create index if not exists schedule_assignment_crew_crew_idx on public.schedule_assignment_crew(crew_id);

alter table public.crew enable row level security;
alter table public.schedule_assignments enable row level security;
alter table public.schedule_assignment_crew enable row level security;

create policy "Allow browser read crew"  on public.crew for select to anon using (true);
create policy "Allow browser insert crew" on public.crew for insert to anon with check (true);
create policy "Allow browser update crew" on public.crew for update to anon using (true) with check (true);
create policy "Allow browser delete crew" on public.crew for delete to anon using (true);

create policy "Allow browser read assignments"  on public.schedule_assignments for select to anon using (true);
create policy "Allow browser insert assignments" on public.schedule_assignments for insert to anon with check (true);
create policy "Allow browser update assignments" on public.schedule_assignments for update to anon using (true) with check (true);
create policy "Allow browser delete assignments" on public.schedule_assignments for delete to anon using (true);

create policy "Allow browser read assignment crew"  on public.schedule_assignment_crew for select to anon using (true);
create policy "Allow browser insert assignment crew" on public.schedule_assignment_crew for insert to anon with check (true);
create policy "Allow browser update assignment crew" on public.schedule_assignment_crew for update to anon using (true) with check (true);
create policy "Allow browser delete assignment crew" on public.schedule_assignment_crew for delete to anon using (true);
```
Seed the crew (or add them via Pricing → Crew):
```sql
insert into public.crew (name, role, color, active, sort_order) values
  ('Adam', 'full_time', '#344236', true, 0),
  ('Johnathan', 'full_time', '#a56543', true, 1),
  ('Peyton', 'intern', '#6e7751', true, 2)
on conflict do nothing;
```
These permissive anon policies mirror the rest of the dev posture and **must be tightened before production** (gate to owner/admin, scope employees to their own schedule) as part of the auth/RLS pass. Employee self-service access to the schedule is deferred until that pass; today the schedule is owner-only.

If you ran an earlier version of this schema (where `schedule_assignments` had a single `crew_id` column), migrate to the join table with:
```sql
create table if not exists public.schedule_assignment_crew (
  assignment_id uuid not null references public.schedule_assignments(id) on delete cascade,
  crew_id uuid not null references public.crew(id) on delete cascade,
  primary key (assignment_id, crew_id)
);

-- Move each existing crew assignment into the join table.
insert into public.schedule_assignment_crew (assignment_id, crew_id)
  select id, crew_id from public.schedule_assignments where crew_id is not null
  on conflict do nothing;

create index if not exists schedule_assignment_crew_crew_idx on public.schedule_assignment_crew(crew_id);

alter table public.schedule_assignment_crew enable row level security;
create policy "Allow browser read assignment crew"  on public.schedule_assignment_crew for select to anon using (true);
create policy "Allow browser insert assignment crew" on public.schedule_assignment_crew for insert to anon with check (true);
create policy "Allow browser update assignment crew" on public.schedule_assignment_crew for update to anon using (true) with check (true);
create policy "Allow browser delete assignment crew" on public.schedule_assignment_crew for delete to anon using (true);

-- Drop the now-redundant column and its index.
drop index if exists public.schedule_assignments_crew_date_idx;
alter table public.schedule_assignments drop column if exists crew_id;
```

## Branding

- Logo: `public/ffe-logo.png` (use this one, not older circular badge logos).
- Business email: freedomfamilyelectric@gmail.com
- Color palette and Tailwind tokens (see `tailwind.config.ts`): pine, deep-pine, moss, sage, clay, sand, cream, stone, charcoal, whitewarm. Radii: `rounded-xl2`, `rounded-xl1`, `rounded-soft`. Shadows: `shadow-soft`, `shadow-card`. Headings use `font-display`.
- Design feel: warm, professional, clean, residential contractor, slightly premium, rounded cards and buttons, earthy palette.
- Writing style: clear, professional, no unnecessary jargon, **no em dashes** in app copy.

### App shell / navigation
`components/app-shell.tsx` is the server component every authenticated page is wrapped in. The header is intentionally minimal: the FFE brand/logo on the left, a "+ New Quote" button, and a **Menu** button on the right. The full tool list lives in a slide-out panel (`components/nav-menu.tsx`, client): clicking Menu slides a 320px (max 86vw) panel in from the right, grouped **Work** (Dashboard, Quotes, Schedule) / **Money** (Receivables, Email Log) / **Setup** (Pricing Admin), each item with an icon and a one-line description, and a Sign out at the bottom. The active tool is highlighted from `usePathname()`. The panel closes on Escape, on backdrop click, and on navigating to a tool; body scroll is locked while open. This replaced an older header that crammed every tool into a single row of links, which ran out of room as tools were added. (The mockup is at `/tmp/ffe-mockups/mockup-nav.html`; a future **Projects** field-stage tracker will join the Work group once its route exists.) `LogoutButton` accepts an optional `className` so the nav foot restyles it full-width.

## Running locally

```bash
npm install
npm run dev
```
Open http://localhost:3000. You need the Supabase env vars in `.env.local`. Deployments happen automatically on push to `main` via Vercel.

## Working with the AI coding assistant

Preferences to follow when making changes:
- Small, step-by-step changes. Push to `main` directly (that is the deploy branch); do not open PRs unless asked.
- For complex files (`quote-builder.tsx`, `review/page.tsx`, `quote-line-item-picker.tsx`), prefer full-file replacements over patch snippets.
- Always give exact file paths, a clear commit message, and explicit desktop + phone test steps.
- Explain every change in plain English with a high-level summary (plan, what was done, how things changed) plus test steps.
- The owner verifies via the live Vercel deployment on phone and desktop.
- No em dashes in user-facing app copy.

## Current state

Done:
- Next.js app foundation, FFE branding shell
- Overview dashboard at `/` (hub): quick actions, summary tiles (active quotes, awaiting payment + outstanding $, paid in full, pricing), unsaved-working-copy resume card, and 5 recent quotes
- Quote lifecycle pipeline at `/quotes` (the quoting tool): five stacked stages (Draft / Prepared / Client Accepted / Pending Payments / Paid in Full) reading saved quotes from Supabase
- Quote builder with pricing calculation, adders, internal notes, and Save as draft
- Mobile layout
- Review page (Prepare writes a prepared quote)
- Active quote browser storage with edit-retains-values
- Supabase save (insert + update), duplicate-save prevention
- Saved quote view (`/quotes/[id]`) and edit (`/quotes/[id]/edit`)
- Delete quote from Supabase
- Owner-only internal notes (not shown to customer)
- Daily-sequence quote IDs assigned server-side at save time: a Postgres `next_quote_id()` function atomically hands out `Q-YYYYMMDD-NNN` (e.g. Q-20260618-001, -002, -003), so two people saving at once can never collide. The builder's Quote ID field is read-only (placeholder "Assigned on save") until the quote is saved; manual entry was removed on 2026-08-11 so the ID is always system-assigned. Editing a saved quote keeps its existing ID. The number is reserved only when the quote is actually saved, so abandoned builder sessions do not create gaps in client-facing IDs.
- Printable Detailed Quote page (`/quotes/[id]/print`) with one-click Download PDF (rendered server-side with react-pdf); the on-screen layout is a preview of the PDF
- Printable Summary Quote page (`/quotes/[id]/summary`) with one-click Download PDF. Condensed customer-facing version: one subtotal per pricing category plus the quote total, no unit prices. Category grouping via `summarizeByCategory` in `lib/calculations.ts`.
- Quote status pipeline: draft, prepared, accepted with manual stage buttons on the `/quotes` pipeline and saved-quote page
- Invoicing from accepted quotes: line items pulled from the pricing catalog (quantity + editable unit price + comment), 50/50 rough-in/finish split (editable), permit fee, two invoices (initial = rough-in % of contract + permit, finish = remainder), paid/unpaid tracking, printable invoices, outstanding balance on the dashboard. The contract is the sum of the line items; the line items are seeded from the quote once then independent on the invoice (`invoice_data.scopeLines`), so the quote stays the point-in-time estimate and final adjustments happen on the invoice. Legacy invoices keep the hand-entered contract until line items are added.
- Pricing admin (`/pricing-admin`): all pricing (line items, pricing levels, contingencies, project types) and business info / default quote notes / invoice payment terms moved out of the static `lib/seed-data.ts` file into Supabase tables, editable in the running app. Deactivate-only (no hard delete), editable sort order, active/inactive badges. Builder and print pages read live from Supabase via `lib/pricing.ts`.
- Accounts Receivable (`/receivables`): collections view over every accepted quote with invoice setup. Two stacked tables — Pending Payments (anything still outstanding) and Historical Paid (paid in full) — one row per job with separate rough-in and finish columns showing amount + paid/outstanding status, plus a job-level total outstanding. Preset period filter (All time / This month / Last 30 days / This quarter / This year) and sort (oldest first / largest outstanding / newest / client). Read-only, derived entirely from `quotes.invoice_data`; no new tables.
- PDF export upgrade: all four customer-facing printables (Detailed Quote, Summary Quote, Initial Invoice, Finish Invoice) download as a real PDF via one-click **Download PDF**, rendered server-side with `@react-pdf/renderer` (`renderToBuffer` streamed back as a file download). Each printable has a react-pdf `<Document>`, a server helper that loads the saved snapshot + live settings and builds pre-formatted props (single source of truth shared by the on-screen preview and the PDF route), and a `/pdf` server route. Shared header/info/total/notes/footer/list building blocks live in `components/pdf/pdf-shared.tsx`. The old browser-print buttons (`print-quote-button`, `invoice-print-button`) and the `.no-print` / `.print-document` print CSS have been removed. react-pdf is kept entirely out of the browser bundle (an earlier client-side attempt blanked the page on load).
- Email PDF from the app: each of the four customer-facing printables has an **Email PDF** button next to Download PDF. It opens a To/Subject/Message panel pre-filled from the customer's email on file + per-doc defaults (editable before send) and posts to `POST /api/email-pdf`, which renders the same PDF buffer the Download route uses and sends it as an attachment via Resend. Quotes (Detailed + Summary) send from `EMAIL_FROM`; invoices (Initial + Finish) send from `EMAIL_FROM_INVOICES` when set, else `EMAIL_FROM`. New `lib/send-pdf-email.tsx` (render dispatcher reusing the `load*PdfInput` helpers + `buildEmailDefaults` + `sendPdfEmail` + `getEmailFrom`), `app/api/email-pdf/route.ts`, `components/pdf/email-pdf-button.tsx` (client), and `components/pdf/pdf-action-bar.tsx` (replaces the old `download-pdf-button.tsx`). Owner one-time setup: add `resend`, verify a sending domain in Resend, set `RESEND_API_KEY` + `EMAIL_FROM` (and optionally `EMAIL_FROM_INVOICES`). The endpoint is open (no auth) until the OAuth pass; see the **Sending email from the app** section.
- Per-adder-line comments (customer-facing): each adder line in the builder has an optional comment box; the comment shows under the item name on the Detailed Quote (on screen + PDF) and in a **Scope of work** section on both invoices (Initial + Finish, on screen + PDF) that lists each quote line with its comment underneath. Stored in `quote_data.lineItems[].comment` (JSONB, no migration). The Summary Quote is category-grouped so per-line comments do not appear there. The invoice scope section shows line names + comments only (no per-line prices, since invoices bill by percentage of the contract — line prices would clash with the charge amounts).
- **Service call / freeform quote type** (a second quote type alongside the catalog-driven new build). `/quotes/new` is now a chooser: **New Build** (the existing per-sqft + catalog + pricing-levers quote) or **Service Call / Other** (freeform manual line items — description + qty + amount + optional comment, no catalog/pricing levers, no squareFootage). A service call has a **single** invoice by default (no rough-in/finish split, no permit), due on completion, emailable, and Stripe-payable; or it can be **split into a Deposit + Final** (reusing the new-build initial/finish kinds, deposit % of contract, paid-deposit freeze) via a "Billing schedule" toggle in the invoice builder — see **Service-call invoicing** above. Its simpler 4-stage lifecycle is Quote / Accepted / Scheduled / Paid, where "Scheduled" is a manual **Mark scheduled** button (a stored status write; NOT auto-derived from `/schedule`, keeping the schedule tool decoupled from stage tracking). Service calls get their own section on `/projects` (5-stage strip: Quote / Accepted / Scheduled / Billed / Paid) and their own 4-stage sections on the `/quotes` pipeline (New builds / Service calls / All toggle). Purpose-built service-call PDFs (quote + invoice) reuse the shared PDF blocks with a Description / Qty / Amount row-amount line table (no unit price). Shared `Q-YYYYMMDD-NNN` quote ids and `INV-NNNN` invoice numbers (no separate sequences). AR: a service invoice is owed (receivable) only when emailed or paid — the finish-invoice rule, NOT the rough-in "owed on setup" rule — and renders as a single "Service" cell with Finish = N/A (a split service call fills both columns: Deposit / Final). Non-breaking: old quotes default to `new_build` and resolve exactly as before. New `quotes.quote_type` column (default `new_build`); the `payments` + `email_log` `invoice_kind` check constraints widened to allow `'service'`. New files: `components/service-quote-builder.tsx`, `components/service-invoice-builder.tsx`, `lib/service-quote-pdf.ts`, `lib/service-invoice-pdf.ts`, `components/pdf/service-quote-document.tsx`, `components/pdf/service-invoice-document.tsx`, `components/service-stage-strip.tsx`, `components/service-project-advance-button.tsx`. Verified: `tsc --noEmit` green + `next build` compiles.

Pending (rough priority):
- ~~Base-rate presets: built-in code fallback~~ **Done (2026-08-15).** The Base Rate dropdown is never empty now even if the `base_rates` Supabase table is missing or unseeded. The quote builder computes `effectiveBaseRates = catalog.baseRates.length > 0 ? catalog.baseRates : DEFAULT_BASE_RATES`, where `DEFAULT_BASE_RATES` (new `lib/base-rates.ts`) is a built-in copy of the $5-$12 presets that mirrors the `supabase/base-rates-table.sql` seed EXACTLY (same ids, names, rateCents, sortOrder) — so a quote that adopts a built-in preset keeps the same id once the DB is seeded and the DB rows take over. The DB always wins when it has any rows; the built-ins are only a floor. Kept in a separate server-import-free module so the client builder can import it without pulling `lib/pricing.ts`'s server Supabase client into the browser bundle. **Pricing Admin does NOT use the fallback** — it shows the real DB state (empty + "add one or run the SQL migration" message) so the owner still sees when the table needs seeding. The builder's dropdown, value lookup, custom-mode check, and `adoptBaseRatePreset` all use `effectiveBaseRates`. Verified: `tsc --noEmit` green.
- ~~Delete invoices without deleting the quote~~ **Done (2026-08-12).** The invoicing page (`/quotes/[id]/invoices`) now has a **Delete invoices** control at the bottom of the "Current invoices" section. It clears `quotes.invoice_data` back to null (removing the two-invoice setup + line items + split + permit) while keeping the quote itself; the quote stays accepted and, with no `invoice_data`, the derived lifecycle auto-returns to **Client Accepted** so the setup can be redone. Confirm flow mirrors `DeleteQuoteButton`. **Guarded:** the button is disabled with an explanation when a payment has been recorded on the job — either any row in the `payments` ledger (card / ACH / manual — real money was taken) OR any invoice flagged paid. A paid invoice is a financial record and clearing the setup would orphan the audit trail; the owner must reverse the payment in Stripe (or mark the invoice unpaid, which also removes its manual ledger row) first. The page computes `paymentCount` (server-side `payments` count for the quote) and `hasPaidInvoice` and passes a `blocked` flag to `components/delete-invoices-button.tsx`. Admin-gating: the invoicing page is already admin-only under RLS (the `quotes` table is admin-only), so only an admin can reach or act on it; no separate role gate was added now (pairs with the future admin-only "reopen invoiced quotes" restriction). Verified: `tsc --noEmit` green.
- ~~Full process/flow review before building the project tracker~~ **Done (2026-08-15).** Walked the entire flow end to end — quote → accept → invoice → pay → schedule → receivables → email — and confirmed the spine is clean (lifecycle, editable invoices, paid-rough-in freeze, delete-invoices guard, Stripe pay flow + double-payment guard + ACH cap, webhook idempotency + refund handling, admin-gated email, append-only email log, middleware auth, receivables derivation). Four rough edges were found and fixed: (A) **dashboard tiles undercounted past 50 quotes** — the tiles were derived from the 50 most-recent rows; they now come from a separate aggregate query (exact count for Active quotes; accepted+invoiced query, cap 500, for the money tiles), with a warning if the money query hits its cap; the recent-rows query dropped to 5. (B) **pipeline hid quotes past 50** — `/quotes` `limit(50)` dropped older quotes from every stage; raised to 500 with a cap-reached warning. (C) **Mark Unpaid could desync the ledger on a Stripe-paid invoice** — the Mark Unpaid button was always shown; it now blocks (with a "refund in Stripe" notice) when a non-manual **succeeded** payment exists for that invoice kind, since reversing real online money is a Stripe refund the webhook handles. Mark Paid stays always available. (D) **receivables `limit(200)`** — raised to 500 with a cap-reached warning so the owner knows to raise it. The flow review's note on the "Reopen as prepared" admin gate: Phase C RLS already makes every quote write admin-only at the DB, so the reopen action is effectively admin-gated today; a UI gate (hide/disable for non-admins) remains a future item with the Phase D login work. Tracker build can now start on a confirmed-clean foundation.
- ~~Searchable adder picker~~ **Done (2026-08-12).** Adding an adder on the **quote** builder and the **invoice** builder now uses a shared searchable `components/catalog-picker.tsx` (type to filter by name/category/unit, click to add) instead of a plain `<select>` dropdown. Already-selected items stay excluded (the builders still compute `availableItems` upstream); Enter adds the lone match, Escape closes/clears, blur auto-closes. Swapped into `components/quote-line-item-picker.tsx` and `components/invoice-builder.tsx`.
- ~~Per-sqft primary pricing + clear lever-scope breakdown~~ **Done (2026-08-12).** The base package price is now driven by a **per-square-foot Base Rate** the owner picks from a dropdown of named presets ($5 through $12, admin-editable in Pricing Admin) or types as a **custom $/sf override** to whatever — this is the primary price lever and replaces the old opaque base-pricing-mode + high-ceiling auto logic (which was hardcoded and not in Pricing Admin). The chosen rate is stored directly on the quote as a snapshot (`quote_data.baseRateCents` + `baseRateLabel` + `baseRateId`), so editing a preset later does not move already-saved quotes; old quotes without the field fall back to the legacy derivation (`deriveLegacyBaseRate` in `lib/calculations.ts`) until re-saved. To kill the "math is buried / I can't see what each lever does" problem, every lever is now tagged with what it moves: the **Base Rate** affects the **Base Package only**; the **Pricing Level** and **Contingency** are multipliers on the **Base Package AND all adders** (except adder lines given a custom unit price, which keep their price and skip the multipliers). These scope tags show inline under each field in the builder, in the **Live Total** panel (`components/quote-totals-panel.tsx`, which also names the pricing level + contingency and counts custom-priced lines), and in a new **Internal Math Breakdown** section on the saved-quote page (`app/quotes/[id]/page.tsx`) that shows the base-package math, adder totals, each multiplier, and the final quote — **not** on any customer-facing PDF (the Detailed/Summary quotes are unchanged and show only the line items + category totals). New admin table `base_rates` (see `supabase/base-rates-table.sql`: table + $5–$12 seed + admin-only RLS mirroring `pricing_levels`), new `BaseRate` type + `catalog.baseRates`, new `components/base-rate-editor.tsx` wired into `/pricing-admin`, and `getBaseRate` in `lib/calculations.ts` now prefers the stored snapshot. The three legacy fields (`basePricingMode`/`manualBaseRateCents`/`highCeilingOrComplexSwitching`) are now optional on `QuoteFormState` and the save payloads write `??` defaults so old rows keep resolving. Owner one-time step: run `supabase/base-rates-table.sql` in the Supabase SQL Editor. Verified: `tsc --noEmit` green.
- ~~Editable per-line unit price on quotes~~ **Done (2026-08-12).** The quote adder unit price was read-only (derived from catalog base x the job's pricing-level/contingency multiplier). Each adder line now has an editable **Unit Price** field (`$` prefix, decimal, default = the derived client price) so Chad can edit per customer; the catalog + pricing level are just the starting point. An edited price becomes a per-line **override** stored on `quote_data.lineItems[].unitPriceCents` (optional, JSONB, no migration; old quotes keep the derived price) and the multiplier no longer applies to that line — i.e. the line snaps to the job's own price and later pricing-level changes leave it alone (mirrors how an invoice line becomes independent once seeded). The catalog list price is still shown ("List $X") beside the field. `lib/calculations.ts` (`calculateLineItem` override branch + a split total: derived lines roll up under the one multiplier pass preserving the original rounding, overridden lines add their absolute totals) and `components/quote-line-item-picker.tsx` + `components/quote-builder.tsx` (`handleUpdateUnitPrice`, passes `clientMultiplier`). On-blur price-change confirmation mirrors the invoice builder (catches accidental edits; Cancel reverts). (The **base package** per-sqft price is now editable too, via the Base Rate dropdown above — see the per-sqft pricing item.) Verified: `tsc --noEmit` green.
- ~~Editable invoices, independent of the quote~~ **Done (2026-08-11, revised same day).** The invoice setup form now has a **Line Items** editor: real priced line items pulled from the pricing catalog (quantity + editable unit price + comment), added from a catalog dropdown. The **contract amount is the sum of (qty × unit price)** of the lines (no longer hand-entered when lines exist); the rough-in/finish % split and permit fee still produce the two invoices, so the % model is preserved and downstream (AR, dashboard, Stripe) is unchanged — they keep reading the stored `contractAmountCents`. Line items are seeded from the quote (names + quantities + client prices + comments) on first setup, then saved on the invoice in `invoice_data.scopeLines` and edited independently of the quote. Line items print on both invoices (qty × unit price = total + comment). Legacy invoices (hand-entered contract, no `scopeLines`) keep working as-is until the owner adds lines; the PDF backfills a display-only scope from the quote until then. A paid invoice's amount is never changed silently: a paid **finish** invoice whose amount changes resets to unpaid with a warning; the paid **rough-in** is frozen and never changes (changes flow to the finish — see the next item); editing only a comment is money-free and never resets anything. This supersedes the old "lock paid rough-in" item below, and **revises the earlier same-day decision** that scope lines would carry "comments only, no per-line amounts" — the owner saw the freeform-text version and asked for real catalog line items with quantity and price instead.
- ~~Split "client" into builder + end customer~~ **Done (2026-08-15) — built as "Project Name" + "Builder / Customer".** The quote's one client field is now two: an optional **Project Name** (the residence / site, e.g. "Fulk Residence") shown as the job name everywhere, and the existing client field relabeled **Builder / Customer** (the party who pays — a builder/GC *or* a direct homeowner customer; keeps its email). Data model kept non-breaking: the existing `clientName` / `clientEmail` keys stay (old quotes resolve unchanged) and a new optional `projectName` joins `QuoteFormState` + a new nullable `project_name` column on `quotes` (owner runs `supabase/project-name-column.sql` once). Save payloads in the builder + review page write `project_name`. UI: builder form has a "Project Name" field above the relabeled "Builder / Customer" + "Builder / Customer Email" fields; the saved-quote, review, and edit pages show a "Project Name" review field + relabeled "Builder / Customer" fields. PDFs: the Detailed + Summary quote "Prepared For" stays the Builder/Customer; the "Project" column now leads with the Project Name (falls back to the address when blank) with the address + type + sqft as the secondary. Invoices: "Bill To" stays the Builder/Customer; the "Project" column leads with the Project Name the same way. The on-screen print previews match. Displays: dashboard recent cards, `/quotes` pipeline cards, receivables rows (column relabeled "Job"), the saved-quote / edit / invoicing page H1s, the dashboard resume card, and the schedule job picker all show Project Name as the primary with Builder/Customer as the sub-line (falling back to Builder/Customer when no Project Name, so old quotes look identical). Email is unchanged — still addressed to the Builder/Customer email. Project Name is optional (spec homes / unfilled / old quotes leave it blank). Verified: `tsc --noEmit` green.
- ~~Project status tracker~~ **Done (2026-08-23).** New `/projects` view follows each accepted job end-to-end as an 8-stage strip: Quote, Rough-in, Rough-in Inspection, Rough-in Billed, Finish, Final Inspection, Final Billed, Paid. Only the **4 field-stage dates** are stored (new nullable `quotes.project_status` JSONB: `roughIn` / `roughInInspection` / `finish` / `finalInspection`, each an ISO date or null); the other **4 stages are derived** from invoicing facts so the tracker can never disagree with `/quotes` or `/receivables`. "Rough-in Billed" / "Final Billed" = that invoice has been emailed (a `sent` `email_log` row) OR paid — the real-world billing action, deliberately distinct from the AR "receivable from setup" rule. "Paid" uses the shared `isPaidInFull(invoiceData, receipts)` so it agrees with every other surface. Field stages advance by a **manual "Mark [stage] complete" button** (stamps today) or by an **Edit stages** panel that back-dates or clears any of the 4 dates; the derived stages hand off to the invoicing flow (link to `/quotes/[id]/invoices`). Filter chips (All / In the field / Billing & payment / Completed) via `?filter=`, with counts. Each card shows the crew from the job's schedule assignments (new batched `fetchScheduleForQuotes` in `lib/projects-server.ts`, read-only alongside the existing schedule fetchers) or "Unassigned". New files: `lib/projects.ts` (client-safe stage types + `computeProjectStages` + `normalizeProjectStatus` + `formatStageDate` (appends T00:00:00 to date-only strings to avoid UTC midnight TZ day-shift) + `todayDateOnly`) + `lib/projects-server.ts` (server-only `fetchScheduleForQuotes` — split out so `lib/projects.ts` stays client-safe for the client advance button, mirroring the `lib/schedule` split), `app/projects/page.tsx` (server, `force-dynamic`, cap 500 + cap warning), `components/project-stage-strip.tsx` (pure presentational 8-step strip; done = sage check, active = clay pulse keyframe in `tailwind.config.ts`, upcoming = muted; horizontal scroll on narrow screens; `prefers-reduced-motion` disables the pulse), `components/project-advance-button.tsx` (client writer), `components/project-stage-editor.tsx` (client 4-date editor). `lib/types.ts` gained `ProjectStatus`. A "Projects" nav entry was added under Work. Owner one-time step: run `alter table public.quotes add column if not exists project_status jsonb;` in the Supabase SQL Editor. No new table / no new RLS (writes go to `quotes`, already admin-only under Phase C). Verified: `tsc --noEmit` green.
- ~~Customer repository~~ **Done (2026-08-15).** Customers are first-class, re-used records linked from quotes (new `customers` table + `quotes.customer_id` FK, backfilled from existing quotes). The quote **keeps its own `client_name` / `client_email` snapshot** (so PDFs, the pay page, Stripe checkout, and point-in-time stability are unchanged); `customer_id` is just the link to the shared record used for autofill and the repository view — so the blast radius is the builder field + a new view, no existing display surface changes. The quote-builder "Builder / Customer" field is now a smart-search `components/customer-picker.tsx` (reuses the CatalogPicker type-ahead pattern + inline create-new via the authenticated browser client): type to match existing customers (name + email), pick to autofill name + primary email + link; type an unknown name to "Create new customer" (inserts the record and links in one flow, without leaving the quote); editing the name away from the linked customer unlinks (name is identity; email edits never unlink or write back). A customer holds **multiple emails** as a JSONB array (`[{ email, label? }]`) — a builder can be a husband/wife team where he gets the quote and she gets the invoice — and there is **no per-email billing flag**: the billing recipient is chosen at send time. The contact-email field on the builder becomes a `<datalist>` of the linked customer's emails when linked. **Email To field accepts multiple comma-separated recipients**: the three print pages load the linked customer's emails and pass them as `suggestedEmails` through `components/pdf/pdf-action-bar.tsx` to `components/pdf/email-pdf-button.tsx` (datalist on the To input); `app/api/email-pdf/route.ts` splits on commas, validates each, and sends to all (Resend `to: array`), logging the comma-joined recipients. New `/customers` list + `/customers/[id]` detail (inline `components/customer-editor.tsx` + the customer's quotes), a "Customers" nav entry, and an optional "Customer record" link on the saved-quote page. New files: `supabase/customers-table.sql`, `lib/customers.ts`, `components/customer-picker.tsx`, `components/customer-editor.tsx`, `app/customers/page.tsx`, `app/customers/[id]/page.tsx`. Backfill is best-effort (two no-email builders with the same name collapse into one; a merge/split tool is future). Owner one-time step: run `supabase/customers-table.sql` in the Supabase SQL Editor. Verified: `tsc --noEmit` green.
- ~~Customer money summary on the Customers views~~ **Done (2026-08-21).** The Customers views now show **Quoted / Invoiced / Paid** per customer, with totals, using the same receivable model as `/receivables` so the figures always tie out. **List page** (`app/customers/page.tsx`): a page-level grand-total band (Total Quoted / Total Invoiced / Total Paid across all customers) plus a per-customer row that replaces the old single "Total quoted" cell with Quoted / Invoiced / Paid (and keeps the quote count). **Detail page** (`app/customers/[id]/page.tsx`): a summary band at the top (customer Total Quoted / Invoiced / Paid) and each job row now shows Invoiced + Paid under its quote total (the aside heading was relabeled "Jobs"). **Definitions** (pinned to the app's existing models): Quoted = sum of `client_quote_total_cents`; Invoiced = `receivableInvoicedCents` with `receipts` from `loadInvoiceReceipts` (a finish invoice that has never been emailed and isn't paid is "scheduled", not counted); Paid = Invoiced minus `outstandingCents` — exactly how `/receivables` derives `totalPaidCents`. Implementation in `lib/customers.ts`: new `computeQuoteMoney` (server-side, reuses `lib/invoice-calculations` + `lib/email-log`), `getCustomerMoney()` replaces `getCustomerStatsMap` (one query across all linked quotes + one batched receipts query, folded in memory, capped at 2000 with a `capped` flag that warns the owner), and `getCustomerQuotes` now selects `invoice_data` + loads receipts and attaches `invoicedCents` / `paidCents` to each `CustomerQuoteSummary`. No schema change (all derived from existing columns). Verified the figures tie out against `/receivables` for a known customer. `tsc --noEmit` green.
- ~~Invoice enhancements: dedicated sequential invoice numbers~~ **Done (2026-08-23).** Each invoice now carries its own sequential number (`INV-0001`, `INV-0002`, ...) assigned once at invoice setup and never changed. A new one-row global counter `invoice_sequences` + security-definer `next_invoice_number()` RPC mirrors the `quote_sequences` / `next_quote_id` pattern (atomic `INSERT ... ON CONFLICT DO UPDATE ... RETURNING`, called from the client; `grant execute to authenticated`). The number lives on a new optional `InvoiceRecord.invoiceNumber` field inside the `invoice_data` JSONB (no new `quotes` column). `components/invoice-builder.tsx` `saveInvoices` reserves a number per un-numbered record via a new client helper `lib/invoice-number.ts` (`nextInvoiceNumber()`, mirrors `lib/quote-id.ts`) and `buildInvoiceRecord` stamps it, preserving an existing number on re-save so editing never burns a gap. **Lazy backfill**: invoices already live/emailed/paid keep their derived `Q-...-R / -F` reference until the setup is next re-saved, so nothing a customer already received changes; a new `invoiceDisplayNumber(quoteId, invoice)` helper in `lib/invoice-calculations.ts` returns `invoice.invoiceNumber ?? invoiceReference(...)` and is used everywhere an invoice is labelled: the PDF header/metadata/footer + download filename (`lib/invoice-pdf.ts`), the print preview, the Stripe checkout product name (`create-checkout-session`), the public pay page, the invoicing-page invoice cards, and the AR Rough-In/Finish cells (the number now shows there). The email subject (`Invoice ${ref} from ${name}`) and `email_log.reference` flow from `pdfProps.reference` automatically. The paid-status write paths (`InvoicePaidButton`, `setInvoicePaid`) spread existing records so the number is preserved on paid/unpaid flips. **Dropped from the brainstorm:** deposit invoice (Chad hasn't asked) and reset-paid (already covered by Mark Unpaid, which clears the flag + the manual `payments` ledger row + unfreezes the rough-in; it intentionally blocks only for real Stripe `succeeded` money, which is a Stripe refund, not an app click). **Deferred to its own phase:** "more than two invoices" → a flexible job-type invoice model (each `project_type` carries an invoice template / milestone set) instead of a free-form N-invoice list; this rewrites the invoice core (AR, Stripe, project tracker, PDFs) and needs Chad's per-job-type billing flows before planning.
- ~~Lock paid rough-in when the quote changes after rough-in payment~~ **Done (2026-08-11) — built as "freeze paid rough-in; changes flow to finish."** Once the rough-in (initial) invoice is marked **paid**, its amount is frozen and never changes on save. Any later edit to the line items, contract amount, or permit fee adjusts the **finish invoice only** — the finish becomes (contract + permit − paid rough-in). The rough-in/finish split is bypassed while the rough-in is paid, so the % fields are locked in the UI and the live preview shows the lock; the initial-invoice PDF labels its rough-in line "paid, locked" instead of a now-stale "X% of contract". This matches how FFE bills: the rough-in is collected during construction and the finish is settled at the end, so only the remaining finish invoice moves. A paid **finish** invoice whose amount changes still resets to unpaid with a warning (so it can be re-marked paid at the new amount); only the paid rough-in is hard-frozen. `computeInvoiceAmounts` (lib/invoice-calculations.ts) branches on the initial invoice's paid status; `components/invoice-builder.tsx` (`buildInvoiceRecord`, `paidAmountChanges`, the disabled % fields + lock note + preview) and `lib/invoice-pdf.ts` (label) follow. Verified: `tsc --noEmit` green.
- Customer payment receipt email. Today the Stripe webhook only writes the `payments` ledger row and flips the invoice's paid flag; the customer gets no receipt from us after paying (Stripe may send its own receipt if the dashboard email setting is on, but test mode does not deliver). A future pass would send a branded receipt email via Resend from the webhook's `payment_intent.succeeded` handler (reusing `lib/send-pdf-email.tsx`'s send path), keyed off the quote's `clientEmail` and the invoice reference, so card and ACH customers both get a confirmation.
- Invoice email: drop the "or ACH" wording for over-$20k invoices. `buildEmailDefaults` in `lib/send-pdf-email.tsx` always says "pay by card or ACH, or mail a check," but ACH isn't offered over `ACH_LIMIT_CENTS` (the checkout route drops `us_bank_account` over the cap), so the email is slightly off for large invoices. Small wiring: thread the invoice `amountCents` into `EmailDefaultsInput` (the invoice print page + `/api/email-pdf` route are the two callers) and branch the message — over the cap, say "pay by card or mail a check" (no ACH); at/under the cap, keep the current card/ACH/check line.
- Invoice-button UX cleanup. The owner flagged that the buttons on an invoice card (View invoice, Mark Paid/Unpaid, Copy payment link) are hard to tell apart / it's not obvious which to choose. Not yet revisited. Revisit the labels, ordering, and grouping (e.g. primary "View invoice," secondary Mark Paid, the pay-link section clearly separated as admin-only). Paired with the earlier "PDF" → "View invoice" relabel + alignment fixes already shipped.
- Owner/admin login (Supabase Auth), one owner + one builder/admin. **Nearly complete** — Phases A, B, C are live (sign-in required, email endpoint gated to admins, all data access through the logged-in session, database fully private). Remaining: Phase D confirms role gates (a paper review; no employee accounts exist yet). The `POST /api/email-pdf` send endpoint is already gated to admins (Phase B).
- Customer "accept" link in the quote email. Today the emailed quote is a PDF attachment only; acceptance is the owner manually clicking Mark accepted after the customer replies/calls. A future pass could embed a signed, unguessable accept link in the email body that flips the quote to accepted via a public endpoint (only that customer can accept that quote). Deferred for now; revisit alongside the auth pass.
- ~~Access-level restriction + accidental-click guardrails on invoiced/paid quotes~~ **Done (2026-08-15).** Two parts. (1) **Access level:** Phase C RLS already makes every `quotes` write admin-only at the DB, so only an admin can reopen or delete any quote today; a separate UI role-gate is moot until non-admin accounts exist (Phase D). (2) **Accidental-click guardrails** (the real fix this pass): the saved-quote page now computes `paymentCount` (server-side `payments` count) + `hasPaidInvoice` + `hasInvoices` (same pattern as the Delete-invoices guard) and feeds them to: **Delete Quote** (`components/delete-quote-button.tsx`, rewritten + tiered) — a quote with no financial state keeps the simple two-step confirm; a quote with invoices or any payment record shows a "Delete is locked" notice (reason tailored to payments / paid invoice / invoices set up), then an "Unlock delete" step, then a **type-the-job-name** confirmation where "Yes, delete permanently" stays disabled until the typed name matches the displayed job name (trimmed, case-insensitive). Hard-delete still cascades to `payments` (the ledger is erased with the quote), so this makes a paid job impossible to wipe by a fat-finger click. **Reopen as prepared** (`components/reopen-quote-button.tsx`, new) replaces the generic `QuoteStatusButton` for the accepted→prepared move only — a confirm dialog with a warning that gets louder with state: no invoices (light), invoices set up (warns the dashboard will show Prepared not Pending Payments), paid invoice / payments (warns a paid job will look un-invoiced). Not a hard block (the owner may legitimately reopen) but never a silent one-click slip. The generic `QuoteStatusButton` is untouched (still used for Prepare, Mark accepted, Move back to drafts — all safe forward/low-risk moves). **Edit page** (`app/quotes/[id]/edit/page.tsx`) now selects `status` + `invoice_data` + `payments` count and shows a warning banner above the builder when the quote is accepted or has invoices/payments (saving there forces status back to `draft` while invoices/payments stay on the row — the banner makes that consequence explicit; the edit page is not linked from accepted quotes, so this covers the direct-URL case). No schema change (reads existing columns). Verified: `tsc --noEmit` green.
- **Review this Pending list on every change** and remove (or mark complete) any item that has been accomplished, so the list stays accurate and does not drift.
- ~~Tighten Supabase RLS for production (remove anon policies, add auth)~~ **Done (Phase C).** `authenticated`/`admin` policies gated by `public.is_admin()` govern every table; all data access goes through the logged-in session; the legacy `anon` policies are dropped, so the database is fully private. `/pricing-admin` is admin-only (its tables are admin-only under the new policies; a team member would see an empty page).

## Recent work (history)

- 2026-08-27: Optional deposit/final split for service-call quotes. Chad is using a service-call quote for a small remodel and wants to bill 50% up front / 50% at finish. Rather than build a free-form N-invoice list (the "more than two invoices" item still deferred — it rewrites the invoice core), this reuses the existing, well-tested new-build two-invoice machinery: a service call can now split into a **Deposit** (`kind: "initial"`) + a **Final** (`kind: "finish"`) invoice, with `permitFeeCents = 0` and a deposit % of the contract (default 50; final = remainder). The split is **optional** — off by default, so a quick troubleshoot/repair job stays a single "due on completion" `kind: "service"` invoice exactly as before, and every service quote saved before this change keeps working unchanged. A "Billing schedule" toggle in `components/service-invoice-builder.tsx` turns the split on/off (with a deposit-% field + live Deposit/Final/Total preview); the toggle is **locked while any invoice is paid** so a shape change can't orphan a paid record. Once the **deposit is paid it is frozen** and later line-item edits flow only to the final (finish = contract − paid deposit), mirroring the new-build paid-rough-in freeze; the deposit-% field is disabled while the deposit is paid. A paid **final** whose amount changes resets to unpaid with a warning (same rule as a paid finish / paid unsplit service invoice). New helper `isUnsplitServiceCall(data)` in `lib/invoice-calculations.ts` (= `quoteType === "service_call"` AND a `kind: "service"` invoice is present); the `computeInvoiceAmounts` service early-return is narrowed to it, so a **split** service call (initial + finish, no `service` invoice) falls through to the existing two-invoice split path with the paid-deposit freeze — no new math. `buildInvoiceRecord` in the service builder mirrors `components/invoice-builder.tsx` (frozen paid initial, reset paid final/service on amount change) and reserves `INV-NNNN` numbers (two for a split, one for unsplit). **Invoicing page** (`app/quotes/[id]/invoices/page.tsx`): unsplit service → one Service card; split service → two cards (Deposit / Final) + a deposit-paid banner + shape-aware "How invoicing works" copy. **PDFs:** `lib/service-invoice-pdf.ts` `loadServiceInvoicePdfInput(id, kind)` now takes a kind (`service` / `initial` / `finish`), sets the title ("Service Invoice" / "Deposit Invoice" / "Final Invoice"), and emits a "previously invoiced (Deposit)" block on the final; `components/pdf/service-invoice-document.tsx` renders it (new optional `previouslyInvoiced` prop). The print page, the PDF route, and `lib/send-pdf-email.tsx` `renderEmailAttachment` now dispatch by **quoteType** (a service call routes ALL of its kinds through the service loader/document; a new build routes initial/finish through the new-build ones) — `fetchQuoteType(id)` first, then branch. **AR** (`app/receivables/page.tsx` + `components/receivables-table.tsx`): a split-service row fills both columns (Deposit / Final captions) with the "Service call" tag; an unsplit service row keeps the single "Service" cell + Finish N/A; new builds unchanged. **Project tracker** (`lib/projects.ts` `computeServiceCallStages`): "Billed" lights when ANY invoice is emailed/paid and "Paid" when the job is paid in full — one generic path covering both shapes (replaces the service-only `findServiceInvoice` branch). **Pay page** (`app/pay/[token]/page.tsx`): selects `quote_type` and relabels the title to "Deposit Invoice" / "Final Invoice" / "Service Invoice" for a service call. Stripe/pay, customers, and P&L are already kind-agnostic — no change. No schema change, no SQL (the `initial`/`finish`/`service` invoice kinds were already allowed by the `invoice_kind` constraints). Verified: `tsc --noEmit` green + `next build` compiles successfully (the pre-existing `/quotes/review` prerender env-var error is local-only; env is present on Vercel).
- 2026-08-25: Builder / Customer phone number on the quote form. Both quote builders (new build + service call) now have a **Builder / Customer Phone** field next to the email. It is autofilled from the linked customer record's `phone` when you pick a customer (same snapshot pattern as the name/email — the quote keeps its own `clientPhone` and an owner-typed value is kept when the customer has none). Phone is stored in the `quote_data` JSONB snapshot (`QuoteFormState.clientPhone`) — no new column (phone isn't used for queries/backfill the way `client_email` is). It shows wherever the email shows: the review page + saved-quote summary (a "Builder / Customer Phone" `ReviewField`), and the customer-facing quote + invoice PDFs + their on-screen print previews (a third line under the name/email in the "Prepared For" / "Bill To" block via a new `leftTertiary` on `PdfInfoGrid`). Optional everywhere — old quotes without it render nothing, exactly like a missing email. The customer picker already loaded `phone` and passed the full `Customer` object to `onSelect`/`onCreated`, so the change is in the builders + the render surfaces, not the picker. No schema change, no migration. Verified: `tsc --noEmit` green + `next build` compiles successfully.
- 2026-08-25: Service call / freeform quote type (a second quote type alongside the catalog-driven new build). The app had one quote type — a catalog-driven new build (per-sqft base rate, pricing level, contingency) with a two-invoice rough-in/finish model — which didn't fit service calls and random one-off jobs. A second, intentionally simpler quote type was added: **service call** = freeform manual line items (description + qty + amount + optional comment), no catalog/pricing levers, no squareFootage. `/quotes/new` becomes a **chooser** (no `?type` → two cards: New Build → `?type=new_build`, Service Call / Other → `?type=service_call`); it resumes the active browser working copy if one exists, routing to the correct builder per stored `quoteType`. The new-build flow is unchanged. Everything is non-breaking: old quotes default to `quote_type = 'new_build'` and resolve exactly as before. **Data model:** new `quotes.quote_type` column (text, not null, default `'new_build'`); the `payments.invoice_kind` + `email_log.invoice_kind` check constraints widened to allow `'service'`; no new table, no new RLS (writes go to `quotes`, already admin-only under Phase C). `lib/types.ts`: `QuoteType = "new_build" | "service_call"`, `QuoteStatus` gains `"scheduled"`, `ServiceLine = { id, name, quantity, amountCents, comment? }`, `InvoiceKind` gains `"service"`, `ServiceLifecycleStage = "quote" | "accepted" | "scheduled" | "paid"`, `QuoteFormState` gains `quoteType` + `serviceLines`, `InvoiceData` gains `quoteType?` + `serviceLines?` (set `quoteType: "service_call"` explicitly on every service invoice save), `ReceivableJob` + `DashboardQuoteRow` gain `quoteType`. **Invoice calc (`lib/invoice-calculations.ts`):** `computeInvoiceAmounts` service branch early-returns BEFORE the rough-in-paid freeze path (single invoice, `totalInvoicedCents = contract`, no split/permit/freeze); `invoiceIsReceivable` kind `"service"` → receivable when paid OR `receipts.service != null` (emailed) — the finish rule, NOT owed on setup; `scheduledCents(data, receipts)` generalizes finish-only `scheduledFinishCents` to cover both finish + service scheduled amounts; `serviceLifecycleStage` maps the 4-stage lifecycle; `invoiceReference` service → `-S` suffix. **Email receipts (`lib/email-log.ts`):** `InvoiceReceipts` gains `service: string | null`; `foldReceipts`/`loadInvoiceReceipts`/`receiptsFromHistory` fold per-kind generically. **Stripe/pay widening:** `lib/pay-token.ts`, `app/api/stripe-webhook/route.ts`, `app/api/email-pdf/route.ts` (`INVOICE_KINDS`), `app/pay/[token]/page.tsx` accept `"service"`; `lib/send-pdf-email.tsx` `renderEmailAttachment` branches on `invoiceKind === "service"` → `loadServiceInvoicePdfInput` + `ServiceInvoicePdfDocument`. **Status-filtered queries widened** from `.eq("status","accepted")` to `.in("status",["accepted","scheduled"])` everywhere service calls should appear: `/receivables`, `/projects`, dashboard money tiles (the active-count query stays draft+prepared — scheduled is past acceptance). New files: `components/service-quote-builder.tsx` (customer picker + project name + address + freeform line editor + totals panel, same localStorage draft→review→prepare flow), `components/service-invoice-builder.tsx` (freeform line editor seeded from the quote's serviceLines then independent, single `kind: "service"` invoice, amountCents = sum of lines, reserves an `INV-NNNN` number, resets paid→unpaid when amount changes), `lib/service-quote-pdf.ts` + `lib/service-invoice-pdf.ts` (server loaders; `fetchQuoteType(id)` is the shared dispatch helper), `components/pdf/service-quote-document.tsx` + `components/pdf/service-invoice-document.tsx` (reuse `pdf-shared` blocks; Description/Qty/Amount line table), `components/service-stage-strip.tsx` (5-node strip), `components/service-project-advance-button.tsx` (Mark scheduled writes status; Billed → link to invoicing; Paid → "Awaiting payment"). **Route dispatch:** the quote + invoice print/pdf routes + the email attachment renderer call `fetchQuoteType(id)` first and branch service → service loaders/documents BEFORE calling new-build-only loaders (so `loadDetailedQuotePdfInput`/`loadInvoicePdfInput` never run on a service row). **`/quotes` pipeline (`app/quotes/page.tsx` + `components/dashboard-quote-section.tsx`):** `?type=` toggle (New builds / Service calls / All); partitions rows by `normalizeQuoteType`; new builds show 5 existing sections, service calls show 4 sections (Quote / Accepted / Scheduled / Paid) using `serviceLifecycleStage`; the Summary quote link is hidden for service calls (summary is new-build-only, route-guarded). **`/projects` (`app/projects/page.tsx` + `lib/projects.ts`):** splits accepted jobs into a New Builds section (existing 8-stage strip + `ProjectAdvanceButton` + `ProjectStageEditor`) and a Service Calls section (5-stage `ServiceStageStrip` + `ServiceProjectAdvanceButton`); `computeServiceCallStages` derives Quote/Accepted/Scheduled from status + Billed/Paid from the service invoice + its email-log receipt + `isPaidInFull` (paid is derived, not a stored status — `QuoteStatus` has no "paid"). Combined bucket counts across both sections. **AR (`app/receivables/page.tsx` + `components/receivables-table.tsx`):** `buildReceivableJob` puts the service invoice in the `initial` column slot with `finish = null`; the table renders a "Service" caption + "Service call" tag for service rows with Finish = "N/A". **Dashboard (`app/page.tsx`):** money-tiles query widened to include "scheduled"; the pending/paid partition uses a kind-agnostic bucket (`computeInvoiceAmounts` + `isPaidInFull`, which both branch on quoteType) so service calls count toward Awaiting payment / Paid in full; recent-quote badges branch on quoteType (`serviceLifecycleStage` vs `lifecycleStage`). **Customers + P&L:** kind-agnostic — `computeQuoteMoney` (customers) and `pnlFromRow` (P&L, legacy full-contract basis) already handle service via the `computeInvoiceAmounts`/`invoiceIsReceivable` branches + the `service: null` receipts default; no change needed. **Chooser + save paths write `quote_type`** on every save (review payload, builder draft, service builder) — the column default is only for old rows. **localStorage:** the chooser reads the active quote; a type-match guard routes a resume to the correct builder; start-new clears the active quote first. Owner one-time SQL (run in Supabase SQL Editor; given inline in chat, no `.sql` file): `alter table public.quotes add column if not exists quote_type text not null default 'new_build';` + the two `invoice_kind` constraint widenings. Verified: `tsc --noEmit` green + local `next build` compiles successfully (the pre-existing `/quotes/review` prerender env-var error is local-only; env is present on Vercel).
- 2026-08-23: Sequential invoice numbers (`INV-0001`). Each invoice now carries a dedicated sequential number assigned once at invoice setup and never changed, independent of the quote id. New one-row global counter `invoice_sequences` + security-definer `next_invoice_number()` RPC (mirrors `quote_sequences` / `next_quote_id`: atomic `INSERT ... ON CONFLICT DO UPDATE ... RETURNING`, called from the client, `grant execute to authenticated`; SQL given inline in chat, no `supabase/*.sql` file). New optional `InvoiceRecord.invoiceNumber` field inside the `invoice_data` JSONB (no new `quotes` column). `components/invoice-builder.tsx` `saveInvoices` reserves a number per un-numbered record via new client helper `lib/invoice-number.ts` (`nextInvoiceNumber()`, mirrors `lib/quote-id.ts`) before the `update({ invoice_data })` write; `buildInvoiceRecord` stamps it, preserving an existing number on re-save so editing never burns a gap. **Lazy backfill** (owner's choice): invoices already live/emailed/paid keep their derived `Q-...-R / -F` reference until the setup is next re-saved, so nothing a customer already received changes; over time everything converges. New `invoiceDisplayNumber(quoteId, invoice)` helper in `lib/invoice-calculations.ts` = `invoice.invoiceNumber ?? invoiceReference(...)`, used everywhere an invoice is labelled: PDF header/metadata/footer + download filename (`lib/invoice-pdf.ts`), print preview (`app/quotes/[id]/invoices/[kind]/print/page.tsx`), Stripe checkout product name (`app/api/create-checkout-session/route.ts`), public pay page (`app/pay/[token]/page.tsx`), invoicing-page invoice cards (`app/quotes/[id]/invoices/page.tsx`), and the AR Rough-In/Finish cells (`components/receivables-table.tsx`, which now shows the number — previously `invoice.reference` was computed but never rendered). Email subject (`Invoice ${ref} from ${name}`) and `email_log.reference` flow from `pdfProps.reference` automatically. Paid-status write paths (`InvoicePaidButton`, `setInvoicePaid`) spread existing records so the number is preserved on paid/unpaid flips. No money math changed. Owner one-time SQL (already run): the `invoice_sequences` table + `next_invoice_number()` function above. Verified: `tsc --noEmit` green + local `next build` compiles successfully.
- 2026-08-23: Project Status Tracker (`/projects`). Every accepted job shown as an 8-stage strip: Quote, Rough-in, Rough-in Inspection, Rough-in Billed, Finish, Final Inspection, Final Billed, Paid. Only the 4 field-stage dates are stored (new nullable `quotes.project_status` JSONB); the other 4 stages are **derived** from invoicing + the `email_log` + `isPaidInFull` so the tracker can never disagree with `/quotes` or `/receivables`. "Billed" = that invoice has been emailed (a `sent` `email_log` row) OR paid — the real-world billing action, deliberately distinct from the AR "receivable from setup" rule; "Paid" uses the shared `isPaidInFull`. Field stages advance by a manual **Mark [stage] complete** button (stamps today) or an **Edit stages** panel that back-dates / clears any of the 4 dates; derived stages hand off to the invoicing flow (link to `/quotes/[id]/invoices`). Filter chips (All / In the field / Billing & payment / Completed) via `?filter=` with counts; each card shows the crew from the job's schedule assignments (new batched `fetchScheduleForQuotes` in `lib/projects-server.ts`, read-only) or "Unassigned". New files: `lib/projects.ts` (client-safe stage types + `computeProjectStages` + `normalizeProjectStatus` + `formatStageDate` (appends `T00:00:00` to date-only strings to avoid UTC midnight TZ day-shift) + `todayDateOnly`) + `lib/projects-server.ts` (server-only `fetchScheduleForQuotes` — split so `lib/projects.ts` stays client-safe for the client advance button, mirroring the `lib/schedule` split), `app/projects/page.tsx` (server, `force-dynamic`, cap 500 + cap warning), `components/project-stage-strip.tsx` (pure presentational 8-step strip; done = sage check, active = clay pulse keyframe in `tailwind.config.ts`, upcoming = muted; horizontal scroll on narrow screens; `prefers-reduced-motion` disables the pulse), `components/project-advance-button.tsx` (client writer), `components/project-stage-editor.tsx` (client 4-date editor). `lib/types.ts` gained `ProjectStatus`; `components/nav-menu.tsx` gained a "Projects" entry under Work. Owner one-time step: run `alter table public.quotes add column if not exists project_status jsonb;` in the Supabase SQL Editor. No new table / no new RLS (writes go to `quotes`, already admin-only under Phase C). Verified: `tsc --noEmit` green.
- 2026-08-21: Customer money summary (Quoted / Invoiced / Paid) on the Customers views. `/customers` now shows a page-level grand-total band (Total Quoted / Total Invoiced / Total Paid across all customers) and a per-customer Quoted / Invoiced / Paid (plus quote count), replacing the old single "Total quoted" column. `/customers/[id]` shows a customer summary band (Total Quoted / Invoiced / Paid) and each job row now carries Invoiced + Paid under its quote total (aside heading relabeled "Jobs"). The figures reuse the **same receivable model as `/receivables`** so they always tie out: Invoiced = `receivableInvoicedCents` with `receipts` from `loadInvoiceReceipts` (a finish invoice that has never been emailed and isn't paid is "scheduled", not counted); Paid = Invoiced minus `outstandingCents` — exactly how `/receivables` derives `totalPaidCents`; Quoted = sum of `client_quote_total_cents`. `lib/customers.ts`: new `computeQuoteMoney` (reuses `lib/invoice-calculations` + `lib/email-log`), `getCustomerMoney()` replaces `getCustomerStatsMap` (one query across all linked quotes + one batched receipts query, folded in memory, capped at 2000 with a `capped` warning), and `getCustomerQuotes` now selects `invoice_data` + loads receipts and attaches `invoicedCents` / `paidCents` per `CustomerQuoteSummary`. No schema change (all derived from existing columns). Verified against `/receivables` for a known customer. `tsc --noEmit` green.
- 2026-08-15: Customer repository (customers as first-class, re-used records). Every quote's Builder / Customer is now backed by a re-usable customer record. New `customers` table (name, `emails` JSONB array `[{ email, label? }]`, phone, note, active) + `quotes.customer_id` FK (`on delete set null`), admin-only RLS via `public.is_admin()` (mirrors `payments-table.sql`). The quote **keeps its own `client_name` / `client_email` snapshot** — `customer_id` is just the link to the shared record used for autofill + the repository view — so PDFs, the pay page, Stripe checkout, and every existing display surface (dashboard, pipeline, receivables, schedule, saved-quote, edit, invoicing, P&L) are **unchanged**. The quote-builder "Builder / Customer" `<input>` is replaced by a smart-search `components/customer-picker.tsx` (reuses the CatalogPicker type-ahead pattern): type to match existing customers (name + email), pick to autofill name + primary email + link; type an unknown name to "Create new customer" (inserts via the authenticated browser client and links in one flow, without leaving the quote); editing the name away from the linked customer unlinks (name is identity; email edits never unlink or write back). A customer holds **multiple emails** (a husband/wife team where he gets the quote and she gets the invoice) with **no per-email billing flag** — the billing recipient is chosen at send time. The contact-email field becomes a `<datalist>` of the linked customer's emails. **Email To field accepts multiple comma-separated recipients**: the three print pages load the linked customer's emails (`getCustomerEmailsForQuote`) and pass them as `suggestedEmails` through `components/pdf/pdf-action-bar.tsx` to `components/pdf/email-pdf-button.tsx` (datalist on the To input); `app/api/email-pdf/route.ts` splits on commas, validates each (`isValidEmail`), and sends to all (Resend `to: array`), logging the comma-joined recipients in `email_log.recipient`. New `/customers` list (name, email, phone, # quotes, total quoted) + `/customers/[id]` detail (inline `components/customer-editor.tsx` for name / emails add-remove / phone / note + the customer's quotes), a "Customers" nav entry under Work, and an optional "Customer record" link on the saved-quote page (uses the `customer_id` column, the source of truth, so backfilled quotes link even before re-save). One-time **backfill** in the same SQL file: one customer per dedupe key (lowercased email, else lowercased name), then link quotes by email (fallback by name). `QuoteFormState` gained `customerId?`; both save paths (builder draft + review prepared) write `customer_id: quote.customerId ?? null`. New files: `supabase/customers-table.sql`, `lib/customers.ts` (`getCustomers`, `getCustomer`, `getCustomerEmailsForQuote`, `getCustomerQuotes`, `getCustomerMoney`), `components/customer-picker.tsx`, `components/customer-editor.tsx`, `app/customers/page.tsx`, `app/customers/[id]/page.tsx`. Modified: `lib/types.ts` (`Customer`/`CustomerEmail` + `customerId?`), `components/quote-builder.tsx` (picker + handlers + datalist + `customer_id` in payload), `app/quotes/review/page.tsx` (`customer_id` in payload), `app/quotes/new/page.tsx` + `app/quotes/[id]/edit/page.tsx` (fetch `getCustomers`, pass `customers`; edit page merges the column's `customer_id` into the initial quote so backfilled quotes open linked), `components/pdf/pdf-action-bar.tsx` + `email-pdf-button.tsx` + `app/api/email-pdf/route.ts` (multi-recipient), the three print pages (`suggestedEmails`), `components/nav-menu.tsx` (Customers entry + icon), `app/quotes/[id]/page.tsx` (Customer record link). Backfill is best-effort (two no-email builders with the same name collapse into one; a merge/split tool is future); no customer delete/deactivate in v1. Owner one-time step: run `supabase/customers-table.sql` in the Supabase SQL Editor. Verified: `tsc --noEmit` green.
- 2026-08-15: Receivables driven by the "actually emailed" date (finish not due until billed). AR was overstating what is owed: both invoices (rough-in + finish) counted as invoiced/outstanding the moment invoicing was set up, even though the finish is created at setup but not actually billed until after the sheetrock gap. The fix uses the existing append-only `email_log` (every invoice email already writes a row with `quote_id`, `doc='invoice'`, `invoice_kind`, `status='sent'`, `sent_at`) as the signal that an invoice is "receivable" — no new field, no new write path, no migration. An invoice is receivable (counts toward AR) when it is paid, or it is the rough-in (always — billed at setup, often handed over in person / collected as cash, not always emailed), or it has been emailed (a `sent` `email_log` row for that quote + kind). The **finish** is therefore *scheduled* (not owed) until emailed or paid: the receivables table shows it in the Finish column as the amount with a "Scheduled / not yet billed" badge and excludes it from the invoiced/outstanding totals; a job with a paid rough-in and an unbilled finish stays in Pending Payments ($0 outstanding, not Historical Paid) because work is still in progress. The receivable date is the first sent email date (fallback `paidAt`, then `issuedAt`) and drives the "Invoiced" date column + oldest-first sort (renamed from "earliest issued"). New server helpers in `lib/email-log.ts`: `InvoiceReceipts` type, `loadInvoiceReceipts(quoteIds[])` (one batched `email_log` query, earliest sent_at per kind), `receiptsFromHistory(rows)` (fold already-fetched email history). `lib/invoice-calculations.ts` gained `invoiceIsReceivable`, `scheduledFinishCents`, `receivableInvoicedCents`, and optional `receipts` params on `outstandingCents` / `isPaidInFull` / `lifecycleStage` — **omitting `receipts` preserves the legacy behavior** (every invoice receivable), so `lib/pnl.ts` (P&L report, full-contract basis) is unchanged. Wired in: `app/receivables/page.tsx` + `components/receivables-table.tsx` (receivable model + "Scheduled" cell), `app/page.tsx` dashboard (Awaiting-payment tile + recent-row badges), `app/quotes/page.tsx` pipeline + `components/dashboard-quote-section.tsx` (stage + card copy, "Finish pending: $Y"), `app/quotes/[id]/page.tsx` + `app/quotes/[id]/invoices/page.tsx` (header "Outstanding / Finish pending / Paid in full" copy, deriving receipts from email history). `lib/types.ts` `ReceivableInvoice` gained `receivable` + `receivableAt`, `ReceivableJob` renamed `earliestIssuedAt` → `earliestReceivableAt` + `scheduledFinishCents`. Edge case: `email_log` started 2026-07-14, so a finish emailed before that (rare, only if still unpaid) reads as scheduled until re-emailed or paid — acceptable. No schema change, no migration. Verified: `tsc --noEmit` green.
- 2026-08-15: Materials cost model rebuilt as simple $ buckets (dropped wire-footage calc + adders %). The first cost-estimate release modeled materials as wire (5 gauges, feet → rolls → $ per roll, seeded from sqft) + a devices line list + an "adders %" markup. Chad doesn't think in footage or a markup knob — he tracks materials as three dollar buckets: wire (~$4500), receptacles & boxes (~$500), panels & feed wire (~$650). And the "adders %" was margin, not cost (margin is already built into the quote's adder line-item prices on the revenue side), so including it on the cost side understated true margin. `CostEstimateData.materials` is now `MaterialBucket[]` (`{ id, name, costCents }`) and `CostEstimateDefaults` is `{ defaultMaterialBuckets: {name}[], defaultHourlyRateCents }` — the wire-gauge defaults, `defaultAdderPercent`, and the `WireDefault` type are all removed. `computeCostEstimate` returns `materialsCostCents` (sum of buckets) and `totalJobCostCents = materials + labor` (no adders line). `normalizeCostEstimate` migrates the two prior shapes: the first-release `wire[]`+`devices[]`+`adderPercent` rolls wire into one "Wire" bucket (sum of ceil(feet/rollLen)×costPerRoll), each device into its own bucket (qty×unitCost), and drops `adderPercent` (it was margin); the `materials[]`+`laborLines[]` shape passes through. `buildDefaultCostEstimate` no longer takes sqft (kept as a voided param for signature stability) — it seeds the default buckets at $0 + one blank labor line. The Job P&L editor (`components/job-pnl-editor.tsx`) materials section is now a list of named $ buckets (name input + $ input + Remove + "Add bucket" + materials subtotal) and the total-job-cost breakdown is just Materials + Labor (no Adders line); the "no costs entered" banner no longer references sqft. The Pricing Admin "Cost Estimate Defaults" section (`components/cost-estimate-defaults-editor.tsx`) is now a default-bucket-names editor (list of name inputs + Add/Remove) plus the default labor rate field — the wire-gauge table and the adder-% field are gone. `lib/types.ts` `AppSettings` doc + the `app/quotes/[id]/pnl` route/schema notes updated. No schema change (the materials shape lives inside the existing `cost_estimate_data` JSONB); no migration to run. Verified: `tsc --noEmit` green.
- 2026-08-15: Guardrails against accidental delete / reopen of invoiced work. The owner flagged that deleting or reopening an invoiced/paid quote was far too easy — a fat-finger click could wipe a paid job or silently demote it. The saved-quote page (`app/quotes/[id]/page.tsx`) now computes the same protection flags the Delete-invoices guard already uses (`paymentCount` from a server-side `payments` count, `hasInvoices`, `hasPaidInvoice`) and feeds them to two rebuilt controls. **Delete Quote** (`components/delete-quote-button.tsx`, rewritten): a quote with no financial state keeps the simple two-step confirm; a quote with invoices or any payment record shows a "Delete is locked" notice (reason tailored: payments recorded / paid invoice / invoices set up), then an "Unlock delete" step, then a **type-the-job-name** confirmation where the final button stays disabled until the typed name matches the displayed job name (trimmed, case-insensitive) — so a paid job (whose `payments` rows cascade-delete with the quote) can never be wiped by a slip. **Reopen as prepared** (`components/reopen-quote-button.tsx`, new) replaces the generic `QuoteStatusButton` for the accepted→prepared move only: a confirm dialog with a state-aware warning that gets louder as the job has more financial state (none → light; invoices set up → warns the dashboard will show Prepared not Pending Payments; paid/payments → warns a paid job will look un-invoiced). Not a hard block (the owner may legitimately reopen) but never a silent one-click slip. The generic `QuoteStatusButton` is untouched (still used for Prepare, Mark accepted, Move back to drafts). **Edit page** (`app/quotes/[id]/edit/page.tsx`) now also selects `status` + `invoice_data` + `payments` count and renders a warning banner above the builder when the quote is accepted or has invoices/payments — saving there forces status back to `draft` while invoices/payments stay on the row, and the banner makes that consequence explicit (the edit page is not linked from accepted quotes; this covers the direct-URL case). No schema change (reads existing columns); RLS already admin-only so no new role gate. Marked the "access-level restriction / reopen invoiced quotes" Pending item done. Verified: `tsc --noEmit` green.
- 2026-08-15: Labor costs rebuilt as per-person crew lines split by rough-in / finish phase. The first cost-estimate release modeled labor as one number (total hours × one rate), which could not capture a real job: Chad's Darren Burke project had four people at different rates (chad 70h @ $70, michael 16h @ $30, adam 22h @ $21, jb 22h @ $15), and FFE tracks rough-in and finish separately because of the gap while sheetrock goes up. `CostEstimateData` now carries `laborLines: LaborLine[]` (replacing `laborHours` + `hourlyRateCents`), where each line is `{ id, person, rateCents, roughInHours, finishHours }`; a line's cost is `rateCents × (roughInHours + finishHours)`. `computeCostEstimate` now returns `roughInLaborCents` + `finishLaborCents` + `laborCostCents` so the view can show each phase independently, and `totalJobCostCents` is materials + labor (the adders % markup was later dropped from cost — see the materials-rebuild entry above). `normalizeCostEstimate` migrates a saved estimate in the old single-rate shape into one labor line (old total hours → rough-in hours at the old rate), so nothing previously saved is lost; `buildDefaultCostEstimate` seeds one blank line at the default rate. The Job P&L editor (`components/job-pnl-editor.tsx`) labor section is now a per-person table (Person / $/hr / Rough-in hrs / Finish hrs / line cost / Remove) with an "Add person" button and phase subtotals (rough-in / finish / labor total). The Pricing Admin "Cost Estimate Defaults" hourly-rate field (now labeled "Default labor rate ($/hr)") is the seed rate for newly added lines. No schema change (the labor shape lives inside the existing `cost_estimate_data` JSONB); no migration to run. Verified: `tsc --noEmit` green.
- 2026-08-15: Internal project P&L (per-job + aggregated). The owner wanted a real P&L — profit/loss per job and rolled up by quarter/year — for internal analysis (not quoting). The only genuinely new data is **job cost**; revenue already existed in three forms (contracted = quote total, invoiced = sum of invoice amounts, paid = invoiced − outstanding). So this adds a cost estimator per job plus two P&L views, all internal-only (never on a customer-facing quote/PDF/invoice/email/pay surface). (1) **Per-job P&L** at `/quotes/[id]/pnl` (linked "Job P&L (internal)" on the saved-quote overview, all statuses): a **revenue basis toggle** (three pill buttons — Contracted / Invoiced / Paid) picks which revenue figure the view calculates under (default Invoiced; flip to Contracted to see projected margin on a not-yet-invoiced job), a P&L summary panel (revenue vs cost vs margin $ + %), and below it the **cost entry editor** (materials as named $ buckets + per-person labor — the original wire-gauge/devices/adders-% model was later rebuilt; see the materials-rebuild entry above). All numbers overridable per job; saved to `quotes.cost_estimate_data` (JSONB). "Reset to defaults" rebuilds from the global defaults (sqft is no longer used). (2) **Aggregated P&L report** at `/pnl` (new "P&L Report" entry in the main nav, under Money): every job grouped by quarter with period subtotals (Σ revenue / Σ cost / Σ margin / blended margin %) and an overall total, the same basis toggle + a period filter (All time / This quarter / This year / Last year). Jobs with no saved cost flagged "est." so they don't read as 100% margin. (3) **Global cost defaults** in a new Pricing Admin section ("Cost Estimate Defaults"): default material bucket names + default labor rate (the original wire-$/roll + roll-lengths + feet-per-sqft + adder-% fields were later replaced by the bucket-names editor — see the materials-rebuild entry above), saved to `app_settings.cost_estimate_defaults` (JSONB) via upsert on the id=1 row. New files: `supabase/cost-estimate-columns.sql` (the two `add column` statements — owner runs once), `lib/cost-estimate.ts` (client-safe calc engine + types + built-in defaults), `lib/pnl.ts` (server-side `pnlFromRow` + `getAllJobPnls` deriving revenue from existing quote/invoice data + cost from the estimate), `components/basis-toggle.tsx`, `components/job-pnl-editor.tsx`, `components/pnl-report.tsx`, `components/cost-estimate-defaults-editor.tsx`, `app/quotes/[id]/pnl/page.tsx`, `app/pnl/page.tsx`. Modified: `lib/types.ts` (`AppSettings.costEstimateDefaults`), `lib/pricing.ts` (`getSettings` reads the new column), `app/pricing-admin/page.tsx` (8th section), `app/quotes/[id]/page.tsx` (P&L nav link), `components/nav-menu.tsx` (P&L Report entry + icon). No new tables, no new RLS (the existing admin-only policies on `quotes`/`app_settings` cover the new columns). Owner one-time step: run `supabase/cost-estimate-columns.sql` in the Supabase SQL Editor. Verified: `tsc --noEmit` green.
- 2026-08-15: Builder/Customer + Project Name split. The quote's single client field is now two: an optional **Project Name** (residence / site, e.g. "Fulk Residence") shown as the job name everywhere, and the existing client field relabeled **Builder / Customer** (the paying party — a builder/GC *or* a direct homeowner; keeps its email). Non-breaking: `clientName`/`clientEmail` keys stay, a new optional `projectName` joins `QuoteFormState`, and a new nullable `project_name` column on `quotes` (owner runs `supabase/project-name-column.sql` once). Builder + review save payloads write `project_name`. Builder form gained a "Project Name" field above the relabeled "Builder / Customer" / "Builder / Customer Email" fields; saved-quote, review, and edit pages show a "Project Name" field + relabeled fields. PDFs (Detailed + Summary quote, both invoices): "Prepared For"/"Bill To" stays the Builder/Customer; the "Project" column now leads with the Project Name (falls back to the address when blank) with address + type + sqft as the secondary; on-screen print previews match. Dashboard recent cards, `/quotes` pipeline cards, receivables rows (column relabeled "Job"), the saved-quote/edit/invoicing H1s, the dashboard resume card, and the schedule job picker all show Project Name primary + Builder/Customer sub (falling back to Builder/Customer when blank, so old quotes look identical). Email unchanged (still to the Builder/Customer email). Project Name optional. Verified: `tsc --noEmit` green.
- 2026-08-15: End-to-end flow review + four fixes (gate before the project tracker). Walked the whole flow (quote → accept → invoice → pay → schedule → receivables → email) and confirmed the spine is clean, then fixed four rough edges. (A) Dashboard tiles (`app/page.tsx`) were derived from the 50 most-recent rows and so undercounted past 50 quotes; they now come from a separate aggregate query — an exact `count` for Active quotes (draft + prepared) and an accepted+invoiced query (cap 500) for the Awaiting-payment and Paid-in-full tiles — with a warning if the money query hits its cap; the recent-rows query dropped to 5. (B) The `/quotes` pipeline (`app/quotes/page.tsx`) `limit(50)` hid quotes older than 50 from every stage; raised to 500 with a cap-reached warning. (C) `InvoicePaidButton` (`components/invoice-paid-button.tsx`) always showed Mark Unpaid; flipping a Stripe-paid invoice unpaid would leave a succeeded ledger row + an unpaid flag (AR showing collected money as owed). It now blocks Mark Unpaid with a "refund in Stripe" notice when a non-manual **succeeded** payment exists for that invoice kind (the page queries `payments` for `method != manual, status = succeeded` per kind and passes a per-invoice `markUnpaidBlocked` flag). Mark Paid stays always available (that direction can't desync). (D) Receivables (`app/receivables/page.tsx`) `limit(200)` raised to 500 with a cap-reached warning. The flow review also confirmed the "Reopen as prepared" action is already admin-gated by Phase C RLS at the DB layer; a UI gate remains a future item with Phase D. Verified: `tsc --noEmit` green.
- 2026-08-15: Base-rate preset built-in fallback. The quote builder's Base Rate dropdown is never empty now even if the `base_rates` Supabase table is missing/unseeded (the original symptom that kicked off the per-sqft pricing follow-up). The builder uses `effectiveBaseRates = catalog.baseRates.length > 0 ? catalog.baseRates : DEFAULT_BASE_RATES`; `DEFAULT_BASE_RATES` (new `lib/base-rates.ts`) is a built-in copy of the $5-$12 presets mirroring `supabase/base-rates-table.sql` exactly (same ids/names/cents/sortOrder), so a quote that adopts a built-in preset keeps the same id once the DB is seeded and DB rows take over. DB always wins when it has rows; built-ins are a floor. Separate server-import-free module so the client builder can import it without pulling `lib/pricing.ts`'s server Supabase client into the browser. Pricing Admin is unaffected and stays truthful to the DB (shows the empty state + "run the SQL" hint) so the owner still sees when the table needs seeding. Verified: `tsc --noEmit` green.
- 2026-08-12: Delete invoices without deleting the quote. The invoicing page (`/quotes/[id]/invoices`) gained a **Delete invoices** control at the bottom of the "Current invoices" section. It nulls `quotes.invoice_data` (clearing the two-invoice setup, line items, split, and permit) while keeping the quote; the quote stays accepted and the derived lifecycle auto-returns to Client Accepted (no invoices) so the setup can be redone, or a mis-built setup can be cleared without losing the quote. Confirm-then-act pattern mirrors `DeleteQuoteButton`. **Guarded:** blocked when a payment has been recorded on the job — any `payments` ledger row (card / ACH / manual) OR any invoice flagged paid — with an explanation (reverse the payment in Stripe, or mark the invoice unpaid which also removes its manual ledger row, first). The page computes `paymentCount` (server-side `payments` count, `head: true`) and `hasPaidInvoice` and passes a `blocked` flag + reason to the new `components/delete-invoices-button.tsx`. No separate admin role gate (the page is already admin-only under RLS). Verified: `tsc --noEmit` green.
- 2026-08-12: Per-sqft primary pricing + clear lever-scope breakdown. The base package is now priced by a **per-square-foot Base Rate** the owner picks from a dropdown of named presets ($5–$12, admin-editable in Pricing Admin) or types as a custom $/sf override — the primary price lever, replacing the old opaque base-pricing-mode + high-ceiling auto logic (hardcoded, not in Pricing Admin, and invisible in the math). The chosen rate is stored as a snapshot on the quote (`quote_data.baseRateCents` + `baseRateLabel` + `baseRateId`), so editing a preset later does not move saved quotes; old quotes fall back to the legacy derivation (`deriveLegacyBaseRate`) until re-saved. Every lever is now tagged with what it moves so the math is legible instead of buried in each line's final price: **Base Rate** → Base Package only; **Pricing Level** + **Contingency** → Base Package AND all adders (except custom-priced adder lines, which skip the multipliers). Scope tags show inline under each builder field, in the Live Total panel (`components/quote-totals-panel.tsx`, now naming the pricing level + contingency and counting custom-priced lines), and in a new **Internal Math Breakdown** section on the saved-quote page — **not** on any customer-facing PDF (Detailed/Summary quotes unchanged). New `base_rates` table (`supabase/base-rates-table.sql`: table + $5–$12 seed + admin-only RLS mirroring `pricing_levels`), new `BaseRate` type + `catalog.baseRates`, new `components/base-rate-editor.tsx` in `/pricing-admin`, and `getBaseRate` (`lib/calculations.ts`) prefers the stored snapshot. The three legacy fields are now optional on `QuoteFormState`; save payloads write `??` defaults so old rows keep resolving. Owner one-time step: run `supabase/base-rates-table.sql` in the Supabase SQL Editor. Verified: `tsc --noEmit` green.
- 2026-08-12: Editable per-line unit price on the quote builder. The quote adder unit price was read-only (derived from catalog base x the job's pricing-level/contingency multiplier); Chad edits per customer, so each adder line now has an editable Unit Price field (`$` prefix, decimal, default = the derived client price) with the catalog list price shown beside it ("List $X"). An edited price becomes a per-line override on `quote_data.lineItems[].unitPriceCents` (optional, JSONB, no migration; old quotes keep the derived price); the multiplier no longer applies to that line, so it snaps to the job's own price and later pricing-level changes leave it alone (mirrors how an invoice line becomes independent once seeded). `lib/calculations.ts` `calculateLineItem` gained an override branch and the grand total splits: derived lines roll up under the one multiplier pass (preserving the original rounding exactly for quotes with no overrides) while overridden lines add their absolute line totals on top. `components/quote-line-item-picker.tsx` renders the field (with an on-blur price-change confirmation that mirrors the invoice builder — Cancel reverts) and `components/quote-builder.tsx` wires `handleUpdateUnitPrice` and passes `clientMultiplier`. Base-package (per-sqft) price is NOT editable yet (only adders). Verified: `tsc --noEmit` green.
- 2026-08-12: Searchable adder picker on the quote and invoice builders. Adding an adder used to be a plain `<select>` dropdown of "Category - Name - $basePrice", a long scroll once the catalog grows. Both builders now use a shared `components/catalog-picker.tsx`: a text input you type into to filter by name, category, or unit type, with a results dropdown (name + category/unit + base price); click to add, Enter adds the lone match, Escape closes and clears, blur auto-closes. Already-selected items stay excluded because the builders still compute `availableItems` upstream. Swapped into `components/quote-line-item-picker.tsx` and `components/invoice-builder.tsx`; the empty state ("all adders already added") is preserved. Verified: `tsc --noEmit` green.
- 2026-08-11: Receivables copy tweak — softened "chase down balances" to "follow up on balances" (the Pending Payments section title and the page intro paragraph). No logic change.
- 2026-08-11: Site-nav re-layout — header collapsed to a slide-out menu. The app header used to cram every tool into one row of links (Dashboard / Quotes / Receivables / Schedule / Email Log / Pricing / Start New Quote / Logout), which ran out of room as tools were added. The header now holds only the brand, a "+ New Quote" button, and a **Menu** button. Menu opens a 320px (max 86vw) panel sliding in from the right with the tools grouped and described: **Work** (Dashboard, Quotes, Schedule), **Money** (Receivables, Email Log), **Setup** (Pricing Admin), each item an icon + label + one-line description, plus a full-width Sign out at the bottom. The active tool is highlighted from `usePathname()` (exact for `/`, prefix match otherwise); the panel closes on Escape, backdrop click, and navigation, and locks body scroll while open. New client `components/nav-menu.tsx` (Menu button + backdrop + slide-out `aside`, inline SVG icons, no icon library). `components/app-shell.tsx` is slimmer (brand + New Quote + `<NavMenu/>`) and no longer imports `LogoutButton`. `components/logout-button.tsx` grew an optional `className` prop so the nav foot can restyle it (the header pill look remains the default). No routes changed; the pending **Projects** field-stage tracker is left out of the menu until its route exists. Verified: `tsc --noEmit` green.
- 2026-08-11: Moved the "How invoicing works" note from a 340px right sidebar to a full-width landscape text box at the top of the invoice page. The invoice page was a two-column grid (builder + invoice cards on the left, the explainer sticky on the right); it is now a single full-width column with the "How invoicing works" note as a wide box above the builder (label + dynamic copy side by side, stacking on mobile). Same `roughInPaid`-conditional copy as before; the sticky 340px aside is gone. `app/quotes/[id]/invoices/page.tsx`. Verified: `tsc --noEmit` green.
- 2026-08-11: Invoice line items pulled from the pricing catalog, with quantity + price + comment (revised same day). The owner saw the first editable-invoices cut (freeform scope lines, comments only) and asked that the invoice "adder lines" pull from the catalog of options like the quote's adder lines do, and carry quantity and price. Rebuilt the invoice setup form's scope editor into a **Line Items** editor: lines are added from a dropdown of catalog pricing items (active, non-Base, grouped "Category - Name"); each line shows the catalog name (read-only) plus editable **Quantity**, **Unit Price** (defaults to catalog base × the quote's combined pricing-level/contingency multiplier), and **Comment**, with a Remove button. The **contract amount is now the sum of (quantity × unit price)** across the lines (shown read-only as "Contract (sum of line items)" when lines exist); the rough-in/finish % split and permit fee still produce the two invoices, so the % model is preserved and AR / dashboard / Stripe are unchanged (they keep reading the stored `contractAmountCents`, which the builder now derives from the lines). Legacy invoices set up before line items (hand-entered contract, no `scopeLines`) keep working as-is: they show the editable Contract Amount field and no lines, and do NOT seed from the quote (so a possibly-paid contract is never silently changed); the owner adds lines to switch to line-item billing. The line items print in a Scope of Work section on both invoices (Initial + Finish, on screen + PDF) showing each line's "qty × unit price = total" and comment: `lib/invoice-pdf.ts` builds the formatted detail/total strings (and backfills a display-only priced scope from `calculation_data.clientFacingLines` for legacy invoices); `components/pdf/invoice-document.tsx` and the invoice print page render them. `invoice_data.scopeLines` shape changed from `{ name, comment }` to `{ pricingItemId, name, quantity, unitPriceCents, comment }` (optional, no migration — legacy invoices have no field). `app/quotes/[id]/invoices/page.tsx` now fetches the pricing catalog (`getPricingCatalog`) and passes `pricingItems` + `clientMultiplier` + a richer `seedScopeLines` (base package → qty 1 @ its total; adders → qty @ client unit price). The base package is a seeded pseudo-line (pricingItemId `base-electrical-package`, not in the catalog dropdown) shown as its true per-sqft detail (sqft × per-sqft rate = total), exactly as the quote shows it. Editing only a comment is money-free and never resets a paid invoice; changing a line's qty/price (and thus the contract) so a paid invoice's amount changes still trips the existing paid-amount warning + reset-to-unpaid. No em dashes in copy. Verified: `tsc --noEmit` green.
- 2026-08-11: Redesigned the invoice page to show the same line-item detail as the quote. The invoice setup form's line items are now an editable **table** with the same columns as the saved-quote page's customer-facing line items — **Item (name + comment) · Qty · Unit · Unit Price · Line Total** — with the contract shown in a table footer ("Contract (sum of line items)"). Quantity, unit price, and comment are editable inline; lines are added from the catalog dropdown and removed per row. The page header now shows a prominent **Contract Total** card (like the saved-quote page's Final Total) and the old duplicate Quote-Reference aside was replaced with a short "how invoicing works" note. `unitType` was added to the scope-line shape so the Unit column matches the quote. Verified: `tsc --noEmit` green.
- 2026-08-11: Invoice line-item table UX: `$` prefix on Unit Price, `qty` label next to the Quantity input, and an immediate price-change confirmation. The owner accidentally edited a unit price instead of a quantity, so the Unit Price input now shows a leading `$` (clearly a dollar field, distinct from the quantity number) and the Quantity input has a `qty` label beside it. As a guard, when the owner leaves a unit-price field whose value no longer matches its starting price, a `window.confirm` pops immediately ("You changed the unit price for X from $A to $B. Keep this price change?"); Cancel reverts the price to its starting value. The starting price is tracked for lines loaded from the saved invoice and for lines seeded from the quote on a first-time setup; lines the owner just added are not tracked (setting their price is expected). `FormattedNumberInput` grew an optional `onBlur` prop for this. All in `components/invoice-builder.tsx` + `components/formatted-number-input.tsx`. Verified: `tsc --noEmit` green.
- 2026-08-11: Finish invoice label + PDF page breaks + `$` spacing. (1) The finish invoice's charge line no longer claims a percentage of the contract (which is wrong once the rough-in is paid and the finish becomes contract + permit - paid rough-in); it now reads "Final Invoice" with the amount. (2) The invoice PDF now keeps each Scope of Work line item together across page breaks: each scope row is `wrap={false}`, so a long scope list flows to a second page cleanly without splitting a single line item (name + comment + total) across a page boundary; the fixed header/footer already repeat on every page and the footer prints "Page X of Y" when multi-page. (3) Moved the `$` on the Unit Price input to the left of the box (outside it, like the `qty` label) so the `$` and the digits never overlap. (The earlier attempt used an in-box `$` with a Tailwind `pl-8` pad, but `.form-input`'s `padding: 0 14px` is unlayered and overrides the utility, so the pad never applied and the `$` sat on the digits.) Verified: `tsc --noEmit` green.
- 2026-08-11: Invoice page clarity pass. (A) The "Live Preview" four-box panel is now invoice-centric and renamed "Invoice amounts (before saving)": the four boxes are **Invoice 1: Rough-In** (initial invoice amount; "X% + permit", or "paid and locked" when paid), **Invoice 2: Final** (finish amount; "Y% of contract", or "balance after rough-in" when paid), **Permit fee (in Invoice 1)** ("collected with rough-in", or "already collected"), and **Total to collect** (contract + permit). The redundant standalone "Rough-in amount" box is gone. (B) The intro paragraph and the "How invoicing works" aside switch to the locked-mode explanation when the rough-in is paid (the old "finish = remainder" copy was wrong once rough-in was paid). (C) "Live Preview" relabeled "Invoice amounts (before saving)" and the invoice cards section relabeled "Current invoices" so pending vs saved amounts are clear. (D) "Finish Invoice" renamed "Final Invoice" everywhere customer-facing (PDF/print doc title, charge line, email panel, and the card title now "Invoice 2: Final"); `KIND_LABEL` now "Rough-In (Invoice 1)" / "Final (Invoice 2)". (E) The header Contract Total card shows a "Rough-in is paid. Changes to line items apply to the final invoice only." hint when locked. Verified: `tsc --noEmit` green.
- 2026-08-11: Comment row spans the full width of each line-item row; paid rough-in is frozen and changes flow to the finish. Two invoice-page changes at Chad's request. (1) Each adder's customer-facing **comment** now sits in its own full-width row beneath the line's Item/Qty/Unit/Unit Price/Line Total columns (a second `<tr>` with `colSpan={6}`), instead of being tucked inside the Item cell, so the note spans the whole bottom of the row. (2) Once the rough-in (initial) invoice is marked **paid**, its amount is frozen and never changes on save; any later edit to the line items, contract, or permit fee flows **only to the finish invoice** (finish = contract + permit − paid rough-in). The rough-in/finish split is bypassed while the rough-in is paid, so the % fields are disabled in the UI and the live preview shows the lock (initial = "frozen — collected", finish sub = "balance after rough-in"); the initial-invoice PDF labels its rough-in line "paid, locked" instead of a now-stale "X% of contract". A paid **finish** invoice whose amount changes still resets to unpaid with the existing warning; only the paid rough-in is hard-frozen. `computeInvoiceAmounts` (lib/invoice-calculations.ts) branches on the initial invoice's paid status; `components/invoice-builder.tsx` (the comment-row table layout, `buildInvoiceRecord` freezing the paid initial, `paidAmountChanges` skipping the rough-in, the disabled % fields + lock note + preview) and `lib/invoice-pdf.ts` (label) follow. No schema change — the lock derives from the existing invoice `status` flag. Verified: `tsc --noEmit` green.
- 2026-08-11: Editable invoice Scope of Work, independent of the quote (first cut, freeform). The invoice setup form (`components/invoice-builder.tsx`) got an editable **Scope of Work** section: a list of line items, each with a name and a customer-facing comment, with add / remove / edit. Seeded from the accepted quote (its `calculation_data.clientFacingLines` — names + comments) the first time invoicing is set up, then saved on the invoice in a new `invoice_data.scopeLines` field and edited there independently of the quote. **This freeform, comments-only version was superseded later the same day** by the catalog line-items + quantity + price version above.
- 2026-08-11: Quote ID field is now read-only in the builder. The owner asked to remove manual entry since the ID is always system-assigned. The Quote ID input is now `readOnly` (placeholder "Assigned on save", helper text "Assigned automatically when you save. It cannot be edited."), so a new quote always mints the daily-sequence number and editing a saved quote keeps its existing ID (the read-only field is prefilled and passed back through `resolveQuoteIdForSave`). The custom-id branch in `resolveQuoteIdForSave` is retained because it is now the edit path (keep existing id); only the manual-entry UI is gone. Comments in `lib/quote-id.ts` and the README sequencing/unique-constraint wording updated. Verified: `tsc --noEmit` green.
- 2026-08-11: Per-adder-line comments (customer-facing). Each adder line in the quote builder now has an optional **Comment (shown on quote)** text box (Chad's request). The comment is stored on the line inside `quote_data.lineItems` (new optional `comment` field on `QuoteLineInput` in `lib/types.ts`; no schema migration — old saved quotes simply have no comment), carried through `calculateLineItem` onto `CalculatedLineItem.comment`, and shown under the item name on the Detailed Quote both on screen (builder customer-facing detail table, review page, saved-quote page, print page) and in the downloaded PDF (`lib/detailed-quote-pdf.ts` passes `comment` into the lines prop; `components/pdf/detailed-quote-document.tsx` renders it as a small italic line under the item). The comments also appear on **both invoices** (Initial + Finish): `lib/invoice-pdf.ts` builds a `scopeLines` array from `calculation_data.clientFacingLines` (name + comment) and `components/pdf/invoice-document.tsx` + the invoice print page render a **Scope of work** section listing each line with its comment underneath. The scope section shows names + comments only (no per-line prices, since invoices bill by percentage of the contract). Empty/whitespace comments render nothing. The Summary Quote is category-grouped so per-line comments do not appear there. The comment input box is width-constrained (flex label + `w-full`/`max-w-full`) and rendered comments use `break-words` so a long comment never pushes the layout sideways. No em dashes in any new copy. Verified: `tsc --noEmit` green.
- 2026-08-11: Designed (not yet built) the project status tracker + site nav re-layout. Two HTML mockups with fake data were produced for review: (1) a new collapsible right-side nav shell (the top menu is getting crowded as tools are added), and (2) a project status tracker view that follows each accepted job through its field lifecycle — quote, rough-in, rough-in inspection, finish, final inspection, plus the existing billing/invoicing steps — in a pizza-delivery-style stage strip. Scope decided this session: a dedicated Projects view (the `/quotes` pipeline keeps doing the quote lifecycle); job costing (payroll + wire/material expenses per job) is a later phase, not v1. No code or schema changes yet; build waits on the owner approving the mockups.
- 2026-07-15: ACH per-payment cap handling. Stripe caps ACH (US bank account) at a per-payment max on the account (currently $20,000; Stripe can raise it with more verification), and FFE invoices can exceed that, so a customer picking ACH on a $25k invoice would have been rejected mid-Checkout. Added `ACH_LIMIT_CENTS` + `achAvailableForAmount` to `lib/payments.ts` as the single source of truth. `/api/create-checkout-session` now offers `us_bank_account` only when the invoice is at or under the cap (over the cap it offers **card only**, no ACH options sent to Stripe); the customer-facing `/pay` page shows a note over the cap explaining the limit and pointing to card or a mailed check. Bump the constant if Stripe raises the limit. Verified: `tsc --noEmit` green.
- 2026-07-15: Double-payment guard (pre-go-live safety). Before flipping Stripe from test to live, added a guard so a customer can't be charged twice on one invoice. `/api/create-checkout-session` now checks the `payments` ledger for an active payment (`processing`/`pending`/`succeeded`) on that invoice before creating a new Stripe Checkout Session and refuses with a clear "a payment is already in progress" message (HTTP 409) if one exists. This closes the live-ACH window: in live mode an ACH sits `processing` for 1-3 business days while the invoice flag is still unpaid (it only flips on `payment_intent.succeeded`, days later), so without the guard a customer reopening the link could pay again and be double-charged. In test mode the window was ~2 seconds, so it never surfaced. A `failed`/`refunded`/cancelled payment does not block; those are retries. New `hasActivePayment` in `lib/payments.ts` (service-role read; fails open on a read error, since the invoice paid flag checked separately is the primary guard). The existing `pay-button` already surfaces any non-ok error message, so the 409 shows the "already in progress" text to the customer. Verified: `tsc --noEmit` green.
- 2026-07-15: Cancelled-payment cleanup (webhook + Mark Unpaid). An ACH the owner cancelled mid-test in the Stripe dashboard left a `payments` row stuck at `processing` forever (the webhook had no handler for the cancel) — harmless but messy, and a real customer could hit the same if they abandon an ACH. `/api/stripe-webhook` now handles `payment_intent.canceled`, marking that row `failed` (it does NOT touch the invoice flag: a cancel is not a refund, and the invoice may be paid by a different successful attempt; reversing a real success stays a `charge.refunded`). The admin **Mark Unpaid** button (`components/invoice-paid-button.tsx`) now also clears any stuck non-terminal Stripe rows for that invoice — after deleting the manual rows, it marks `processing`/`pending` Stripe rows `failed` (explicit admin override so the ledger matches the now-unpaid flag and a fresh payment can be taken). It still never touches a `succeeded`/`refunded` Stripe row (real money; a refund is handled in Stripe → `charge.refunded` via the webhook). Owner one-time: add `payment_intent.canceled` to the webhook endpoint's event list in the Stripe dashboard (alongside the existing events). Verified: `tsc --noEmit` green.
- 2026-07-15: ACH fix + invoice email copy. Two follow-ups to the Stripe step-3 wiring. (1) ACH payments were landing "incomplete" in the Stripe dashboard because the customer could pick "enter bank manually" in Checkout, which falls back to microdeposit verification that can't complete inside a one-shot Checkout. `/api/create-checkout-session` now sets `payment_method_options.us_bank_account.verification_method = "instant"` (plus the existing Financial Connections `payment_method` permission), which forces the instant bank-login (Plaid) path and removes the manual-entry option entirely, so ACH settles normally. (2) Invoice email body (the copy pre-filled into the Email PDF panel when a pay link is live) now tells the customer they can pay online with a credit/debit card or by ACH bank transfer using the link, or mail a paper check (mailing address is on the invoice), instead of the generic "pay by the method shown on the invoice" line. No em dashes (house style). Verified: `tsc --noEmit` green.
- 2026-07-15: Stripe online payment wired (step 3 — card + ACH, test keys). Installed `stripe` and filled in the two route skeletons. `/api/create-checkout-session` now creates a real Stripe Checkout Session (`mode: payment`, `payment_method_types: ["card", "us_bank_account"]`) with the amount re-read from the DB, success/cancel URLs back to `/pay/success` + `/pay/canceled`, `customer_email` pre-filled from the quote when present, and `metadata: { quote_uuid, invoice_kind }` so the webhook can find the invoice. `/api/stripe-webhook` verifies the Stripe-Signature header with `STRIPE_WEBHOOK_SECRET` (bad signature -> 400 so Stripe drops it instead of retrying forever; handler error -> 500 so Stripe retries), then dispatches: `checkout.session.completed` creates the `payments` row via upsert (method resolved by expanding the payment intent's payment_method; card = `succeeded` + flip flag, ACH = `processing`), `payment_intent.succeeded` marks `succeeded` + flips the flag (the ACH success that lands days later), `payment_intent.processing` records processing, `payment_intent.payment_failed` records failed (invoice stays unpaid), `charge.refunded` marks refunded + reverses the flag. New `lib/payments.ts` (server-only, service-role) does the idempotent upsert by `stripe_payment_intent_id`, status update, invoice-flag flip, amount re-read, and the `stripe_webhook_events` audit insert. New `supabase/stripe-webhook-table.sql` (owner one-time): the `stripe_webhook_events` table (admin-select RLS; webhook writes via service-role) + a unique index on `payments.stripe_payment_intent_id` (NULLs distinct, so manual payments are unaffected). Typed the admin service-role client as the same loose type the browser/server clients use (the bare `createClient` default generic resolves rows to `never`, rejecting plain-object writes; the cast matches the existing no-generated-types convention where reads cast `data as RowType` and writes pass plain objects). Owner one-time setup to turn it on (test mode): run the webhook SQL, set `STRIPE_SECRET_KEY` + `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` (test keys) in Vercel, enable US bank account in the Stripe dashboard, register the webhook endpoint to get `STRIPE_WEBHOOK_SECRET`, redeploy, then test with the test card `4242...` and a test bank. Verified: production build green. Step 4 (live keys) waits on the owner's Stripe business account + banks.
- 2026-07-15: Customer online payment plumbing (Stripe step 2 — no Stripe keys/package needed). Built the public pay-link plumbing end to end so step 3 is just "add keys + register the webhook." New `lib/pay-token.ts`: stateless HMAC-signed tokens (base64url payload `.` base64url HMAC over `{q: quoteUuid, k: kind}`) signed with a server-only `PAY_LINK_SECRET`; `createPayToken`/`verifyPayToken` (constant-time compare)/`buildPayUrl` (absolute URL via `APP_URL`)/`paymentsConfigured()`. New public pages `app/pay/[token]` (verifies the token, re-reads the invoice + amount from the DB via the service-role client so the amount shown/charged always comes from our DB, renders a branded payment page with a Pay button, and shows "already paid" / "no balance" / "invalid link" states), `app/pay/success`, `app/pay/canceled`. New `app/api/create-checkout-session/route.ts` (POST, public; verifies token, re-reads amount from DB, returns a Stripe Checkout URL once Stripe is wired, else `{ ok: false, configured: false }` so the button shows an honest "being set up" message) and `app/api/stripe-webhook/route.ts` (POST, public; skeleton returning 503 until `STRIPE_WEBHOOK_SECRET` is set; step 3 fills signature verification + idempotent `payments` insert + invoice flag flip + ACH delayed-settlement/return handling). New `components/pay/pay-button.tsx` (client; posts the token, redirects to Stripe or shows "being set up") and `components/pay/copy-pay-link-button.tsx` (client; admin invoicing page; copies the server-minted pay URL + a Preview link). The admin invoicing page (`app/quotes/[id]/invoices/page.tsx`) mints a pay URL per invoice and shows the copy-link button when `PAY_LINK_SECRET` + `APP_URL` are set. Invoice emails now append a "Pay online here: <link>" line, but only when `paymentsConfigured()` (Stripe wired too), so customers never get a dead link before payment actually works (`buildEmailDefaults` in `lib/send-pdf-email.tsx` takes new `quoteUuid`/`invoiceKind`; the email-pdf route + the invoice print page pass them). Middleware already listed `/pay` and `/api/stripe-webhook` as public (Phase B), so no middleware change. Owner one-time setup (step 2 testing only — no Stripe needed): set `APP_URL` (the public site origin) and `PAY_LINK_SECRET` (`openssl rand -hex 32`) in Vercel, then use the new Copy payment link button on an invoice to preview the `/pay` page. Verified: production build green. No new DB tables. Step 3 (Stripe keys) remains.
- 2026-07-15: Payments ledger (Stripe step 1 — needs no Stripe keys). Added a new `payments` table as the real ledger of money received (audit trail that survives a flag flip); the per-invoice paid flag in `quotes.invoice_data` stays the UI source of truth for now. Upgraded the manual **Mark Paid** button (`components/invoice-paid-button.tsx`) so marking an invoice paid flips the flag (primary) and then best-effort inserts a `payments` row (`method = 'manual'`, `status = 'succeeded'`, `recorded_by` = the signed-in admin's email, `paid_at` = now); marking unpaid reverses it by deleting the matching manual row(s) for that invoice (Stripe card/ACH rows are never touched). The two writes are non-atomic across two queries (a known v1 limitation; a future Postgres RPC can do both in one transaction); if the ledger write fails the flag still flips and a warning shows so the owner can retry. The admin email is threaded from the server: `app/quotes/[id]/invoices/page.tsx` now reads `getServerUser()` in parallel with the quote query and passes `recordedBy` down through `InvoiceCard` to the button (no extra client getUser round-trip). The `payments` table has admin-only RLS (select/insert/update/delete gated by `public.is_admin()`); the future Stripe webhook will write via the service-role client which bypasses RLS, so it needs no policy. Owner one-time SQL: `supabase/payments-table.sql` (safe to re-run). Verified: production build green. Foundation for Stripe card + ACH (next steps). No new env vars.
- 2026-07-15: Supabase Auth — Phase C3 (drop the legacy anon policies; database now fully private). Dropped every build-phase `to anon` policy across `quotes`, `email_log`, the four pricing tables, `app_settings`, `crew`, and the two schedule tables, and revoked the anon grant on `next_quote_id`. After this, anonymous direct access to the database is denied — only a logged-in admin can read/write quotes, pricing, invoices, email log, and settings; any logged-in user can read the schedule (writes admin-only). The first pass (`supabase/phase-c-3-drop-anon-policies.sql`) dropped by name and missed 14 policies on the pricing tables + `app_settings`: their real DB names embed the specific table name (`pricing_items`/`pricing_levels`/`contingency_options`/`project_types`/`app_settings`) rather than the generic `pricing`/`settings` the README documented, and `drop policy if exists` with a non-matching name is a silent no-op. Caught by the `pg_policies` verification query. `supabase/phase-c-3b-drop-remaining-anon.sql` finished the job by reading `pg_policies` and dropping every anon policy in the public schema directly (no name guessing). Also fixed the README's pricing/app_settings policy block to use the real table-specific names so this drift can't bite again. Verified: zero `{anon}` rows in `pg_policies`; app unchanged for logged-in admins. Rollback if ever needed: `supabase/phase-c-rollback-re-add-anon.sql`. Phase D (role-gate confirmation) remains as a paper review.
- 2026-07-15: Supabase Auth — Phase C1 + C2 (role-aware RLS + switch data access to the logged-in session). **C1** added `authenticated`/`admin` RLS policies alongside the existing `anon` ones for every table, gated by a new `public.is_admin()` security-definer helper that reads `auth.users.raw_app_meta_data.role` (the same field `lib/auth.ts` reads, so server gates and database rules agree — no second source of truth, no JWT-claim dependency). Schedule tables (`crew`, `schedule_assignments`, `schedule_assignment_crew`) allow any authenticated user to read (for future crew) but admin-only writes; everything else is admin-only. `next_quote_id` was granted to `authenticated` (the anon grant stays until C3). SQL: `supabase/phase-c-1-add-policies.sql` (safe to re-run; drop-if-exists + create-or-replace). **C2** swapped all data access off the bare anon client: server pages + `lib/pricing`/`lib/email-log`/`lib/*-pdf` use `getSupabaseServer()` (per-request, RLS-enforced via the session); 13 client components + `lib/quote-id` use `getSupabaseBrowser()` (session-carrying singleton, module-level so existing `supabase.` calls are unchanged); the keepalive route uses a new server-only `lib/supabase-admin.ts` service-role client (no user session; bypasses RLS). `lib/schedule` was split into a shared `lib/schedule.ts` (types + pure helpers + fetcher cores that take a client arg) plus `lib/schedule-server.ts` and `lib/schedule-client.ts` wrappers, so `cookies()`/`next/headers` never lands in a client bundle (the board's week navigation reads via the browser wrapper). The bare anon client `lib/supabase.ts` was removed. Anon policies still exist, so the app kept working throughout — the canary was the dashboard showing quotes (confirms `is_admin()` resolves). Requires the `SUPABASE_SERVICE_ROLE_KEY` env var on Vercel (keepalive). Verified: production build green (types + client/server bundle boundaries clean); tested live (all reads/writes work for a logged-in admin; keepalive 200). Rollback: revert commit `bfdc443` (re-adds the anon client; anon policies still present). Remaining: C3 drops the anon policies (database fully private), D confirms role gates.
- 2026-07-15: Supabase Auth — Phase B (front-door lock + email-endpoint gate). `middleware.ts` now redirects unauthenticated users to `/login` for every page except public routes (`/login`, `/pay/*`, `/api/keepalive`, `/api/stripe-webhook`); logged-in users are never sent to `/login` (visiting `/login` while signed in redirects to `/`). API routes are not redirected (each enforces its own auth). `app/api/email-pdf/route.ts` is gated to an admin session via `getServerUser()` from `lib/auth.ts` (401 unauthenticated, 403 non-admin) — closing the open-endpoint hole where anyone with the URL could email as the business. RLS is still permissive `anon` (unchanged), so the database is not yet private; Phase C will tighten it. Verified: `tsc` clean + production build green; tested live (no lockout for logged-in admins, locked redirect for logged-out, email send still works for admins, `/login` redirects to `/` when signed in). Rollback commit `4110581`. No new env vars.
- 2026-07-15: Supabase Auth — Phase A (login infrastructure, app still open). Added `@supabase/ssr` and the auth-aware clients: `lib/supabase-env.ts` (lazy shared env reader, fails at call time not build time), `lib/supabase-browser.ts` (singleton browser client carrying the session), `lib/supabase-server.ts` (server client reading cookies; `setAll` is a no-op in the read-only server-component context since middleware refreshes the session), and `lib/auth.ts` (`getServerUser` / `isLoggedIn` / `isAdmin`; role read from `app_metadata.role`, defaulting to `team_member`). New `middleware.ts` calls `getUser()` on every non-static route to refresh the session but does **not** redirect yet — the app stays fully open. New `app/login/page.tsx` + `components/login-form.tsx` (email/password via `signInWithPassword`, redirect to `/` on success) and `components/logout-button.tsx` (added to the app header). Roles: `admin` (owner + business owner, full) and `team_member` (crew, schedule-only — wired in a later phase); framework is role-aware from day one so adding employees later needs no rewrite. Phase A is verified: `tsc` clean and production build green (`/login` + edge middleware compile). Existing data access via `lib/supabase.ts` (anon client) is untouched — no breakage. **One-time Supabase setup** (in dashboard): enable Email provider, add the two admin users, set `app_metadata.role = "admin"` (SQL provided). Remaining phases: B (front-door redirect + gate `/api/email-pdf`), C (role-aware RLS tightening), D (role-gate confirm). No new env vars (uses existing `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY`).
- 2026-07-15: Supabase keepalive ping. Added `GET /api/keepalive` (`app/api/keepalive/route.ts`, public + unauthenticated) that runs one trivial Supabase query (a head-count on `quotes`) and returns 200, or 503 if the query errors; and `.github/workflows/keepalive.yml`, a daily GitHub Actions cron (~08:17 UTC, also runnable on demand) that `curl -f`s that route so the Supabase free-tier project is not paused after 7 days of inactivity. The route reads/writes nothing sensitive (counts rows only). Scheduled workflows run only on the default branch, so this is effective on `main`. The production URL (`https://admin.freedomfamilyelectric.com`) is **hardcoded in the workflow** — no repository variable or setup is needed; if the URL changes, update `APP_URL` in the workflow file. Runs/failures are visible in the Actions tab → "Supabase keepalive". No SQL, no env vars, no new dependencies.
- 2026-07-14: Email audit trail. Every send through `/api/email-pdf` now appends one row to a new append-only `email_log` table (both sent and failed attempts). The row records the quote, doc type, invoice kind, reference + doc-title snapshots, recipient, subject, Resend message id, status, error, and timestamp. New `lib/email-log.ts` (`logEmailSend` best-effort insert, `getEmailHistoryForQuote`, `getRecentEmailLog`). The `/api/email-pdf` route calls `logEmailSend` after each send; a logging failure never changes the send response. Two read-only views: an **Email History** section on the saved-quote page (`app/quotes/[id]/page.tsx`, per-quote, newest first) and a new global `/email-log` page (`app/email-log/page.tsx`, nav link in `components/app-shell.tsx`, 50 most recent sends across all quotes). `reference`/`doc_title` snapshots + `on delete set null` keep history readable even if the quote is deleted. Owner one-time SQL: create `email_log` + indexes + permissive anon read/insert policies (tighten with the auth pass); see the **Email audit trail** section. Appends only, no update/delete.
- 2026-07-14: Email quotes and invoices directly from the app. Each of the four customer-facing printables (Detailed Quote, Summary Quote, Initial Invoice, Finish Invoice) now has an **Email PDF** button next to the existing Download PDF. It opens a small To / Subject / Message panel pre-filled from the customer's email on file (`quote_data.clientEmail`) plus per-doc defaults (no em dashes in the copy), editable before sending; Send posts to `POST /api/email-pdf`, which renders the requested PDF to a buffer using the **same** `load*PdfInput` + `renderToBuffer` path the Download routes use and attaches it to an email sent via Resend. Quotes (Detailed + Summary) send from `EMAIL_FROM`; invoices (Initial + Finish) send from `EMAIL_FROM_INVOICES` when set, else `EMAIL_FROM` (resolved by `getEmailFrom`). The emailed PDF is byte-identical to the downloaded one and matches the on-screen preview; internal notes stay excluded. New server-only `lib/send-pdf-email.tsx` (a render dispatcher reusing the existing `lib/detailed-quote-pdf.ts` / `lib/summary-quote-pdf.ts` / `lib/invoice-pdf.ts` helpers, plus `sendPdfEmail`, `getEmailFrom`, `buildEmailDefaults`, and `isValidEmail`), new `app/api/email-pdf/route.ts` (POST, `force-dynamic`; validates `id` / `doc` / `invoiceKind` / `to`, resolves the from address by doc type, renders, sends, returns ok/error JSON), new client `components/pdf/email-pdf-button.tsx` (inline panel, sending/sent/error states, styled to match the download button), and new server `components/pdf/pdf-action-bar.tsx` (the printable toolbar: Back link + Download PDF anchor + Email PDF button, replacing the deleted `components/pdf/download-pdf-button.tsx`). The three print pages (`app/quotes/[id]/print/page.tsx`, `app/quotes/[id]/summary/page.tsx`, `app/quotes/[id]/invoices/[kind]/print/page.tsx`) now compute email defaults server-side from the data they already load and pass them to `PdfActionBar`. `resend` added to `package.json`; README env section, Routes table, file tree, and a new "Sending email from the app" section all updated. Owner one-time setup: run `npm install`, verify a sending domain in Resend (add its DNS records), set `RESEND_API_KEY` and `EMAIL_FROM` (an address on the verified domain) in Vercel + `.env.local`, and optionally `EMAIL_FROM_INVOICES` for a separate invoice from address. The email's Reply-To is the business email on file (`app_settings.business_email`), so customer replies land in the owner's inbox. **Security caveat:** `/api/email-pdf` is intentionally open (no auth) for now; it is gated to the owner/admin session in the pending OAuth pass. No audit trail is stored this pass. Marked the "email the generated PDF" Pending item complete.
- 2026-06-22: Added a scheduling tool (Phase 1: phone-first week list). New `/schedule` route (owner-only; employee self-service is deferred to the auth pass) where the owner schedules his crew (Adam, Johnathan full-time; Peyton intern) onto rough-in and finish jobs for accepted quotes, plus free-form service calls. Two new tables — `crew` (editable list managed under Pricing → Crew; deactivate-only) and `schedule_assignments` (one row per day; `quote_id` and `phase` both optional so a service call has no quote/phase; `title`/`location` stored as an editable snapshot; optional clock times default all-day; status scheduled/completed/cancelled) — plus a `schedule_assignment_crew` join table so an entry can have **one or more** crew members, with permissive dev RLS policies to be tightened alongside the auth pass. New `lib/schedule.ts` (types + server fetchers `getCrew`/`getScheduleRange`/`getSchedulableJobs` + time/phase/overlap helpers), `app/schedule/page.tsx` (server, `force-dynamic`, fetches the current week), `components/schedule-board.tsx` (client week board: Prev/Today/Next nav, 7-day list that becomes a 7-column grid on desktop, add/edit modal, refetches the week on nav and after every save), `components/schedule-assignment-form.tsx` (client form with a Quote-job vs Service-call toggle; multi-select crew pills; picking a quote pre-fills title/location and reveals a Rough-In/Finish phase; soft overlap warning), and `components/crew-editor.tsx` (admin editor mirroring the pricing editors). Added a Schedule nav link. Phase 2 (desktop week grid with native HTML5 drag-to-reschedule) to follow. Owner must run the crew + schedule_assignments + schedule_assignment_crew SQL (DDL + policies + seed) in the Supabase SQL Editor before testing.
- 2026-06-22: Scheduling Phase 2 — desktop drag-to-reschedule. On the desktop 7-day grid, assignment cards are now native HTML5 draggable; drop a card onto another day column to move its `work_date` (no edit form needed). The source card dims while dragging and the target day column highlights. Tap-to-edit remains the always-available fallback and is the only path on mobile (HTML5 drag is mouse-centric). Drag changes day only (crew/phase/times are still edited via the form) — a deliberate simplification once multi-crew made the old "crew as rows" grid model no longer fit (one entry can span several crew). No schema change and no SQL to run; `work_date` is a plain column and the move is an `update({ work_date })` plus a week refetch. Single-file change (`components/schedule-board.tsx`).
- 2026-06-22: Minor code-hygiene pass (internal, no user-facing change). (1) Hoisted the duplicated `normalizeStatus` helper (was copy-pasted in `app/page.tsx`, `app/quotes/page.tsx`, and `app/quotes/[id]/page.tsx`) into a single `normalizeStatus` export in `lib/types.ts` next to the `QuoteStatus` type; the three pages now import it. (2) Removed the dead `isFullyPaid` function from `lib/invoice-calculations.ts` and the unused `isFullyPaid` field from the `ReceivableJob` type + its assignment in `app/receivables/page.tsx`. `isFullyPaid` (per-invoice paid flags) was superseded by the balance-based `isPaidInFull` in the 2026-06-22 reconciliation work but kept only for this unused field; the AR table already partitions off `totalOutstandingCents === 0 && totalInvoicedCents > 0`, which is `isPaidInFull`, so `isPaidInFull` is now the single "paid in full" definition with no leftover. (3) Switched the settings editor (`components/settings-editor.tsx`) from `.update().eq("id", 1)` to `.upsert({ id: 1, ... }, { onConflict: "id" })` so saving business info self-heals if the `app_settings` id=1 seed row is ever missing (previously the update silently matched 0 rows and reported "Settings saved" while saving nothing). The upsert's insert path requires an `app_settings` insert anon policy, which was missing from the README policy block and has been added; run the one-line `create policy ... for insert` SQL below if it has not been run. (4) Added `export const dynamic = "force-dynamic"` to `app/quotes/[id]/page.tsx` and `app/quotes/[id]/invoices/page.tsx`, which were relying only on the shared client's `cache: "no-store"`; the README's "all live-data pages force-dynamic" claim is now literally true.
- 2026-06-22: Multi-page PDFs now repeat the header and footer on every page. Previously a quote or invoice that spilled onto a second page rendered page 2+ bare (no logo/business name at the top, no contact line at the bottom) because the header and footer were ordinary in-flow content that only rendered once. The header and footer `<View>`s in `components/pdf/pdf-shared.tsx` (Summary Quote + Invoice) and `components/pdf/detailed-quote-document.tsx` (Detailed Quote) are now `fixed` and absolutely pinned (top:0 / bottom:0), and the page top/bottom padding was raised (48 -> 112 top, 48 -> 56 bottom) to reserve space so the flowing line-item content never slides under the repeated header/footer. The footer also appends "Page X of Y", but only when the document is actually more than one page (a 1-page quote still shows just the contact line, no "Page 1 of 1"). Owner to visually verify on Vercel with a 2-page Detailed Quote. Remaining known limitation (not addressed): on a 2-page Detailed Quote the line-items table itself splits at the page break, so the bordered box and the column-header row (Item / Qty / Unit / Unit Price / Line Total) do not repeat on page 2 — the rows continue but without column headers.
- 2026-06-22: Cosmetic polish pass. (1) Removed em dashes from user-facing app copy (README says none allowed): invoice card titles ("Invoice 1: Rough-In (Initial)"), receivables placeholders ("N/A" instead of an em dash for missing issued date / missing invoice), pricing-admin helper text, and the pricing-level description fallback. (2) Invoice PDF filename now prefixes `invoice-` (e.g. `invoice-Q-20260619-001-R.pdf`) so it is recognizably an invoice in a downloads folder. (3) The initial invoice PDF no longer prints a "Permit Fee $0.00" line when there is no permit fee; the on-screen preview matches (both render from the shared `lib/invoice-pdf.ts` props). (4) Fixed preview-vs-PDF drift: the on-screen notes/terms boxes on the Detailed Quote, Summary Quote, and invoice previews are now hidden when empty (matching the PDFs, which already hid them); the Detailed Quote preview now shows a "No priced items on this quote." row when empty (matching the PDF); and the Detailed Quote preview no longer leaves a leading " · " when project type is blank (now uses the same `filter(Boolean).join(" · ")` the Summary and invoice previews use).
- 2026-06-22: README drift cleanup (documentation only, no code). Removed the stale "temporary build-status panel" from the `/quotes` route description. Rewrote the Detailed Quote and Summary Quote "Done" bullets (which still said "browser print dialog, no PDF dependency yet") to reflect the completed one-click Download PDF. Updated the Internal Notes section (PDF export is no longer "coming later"; it exists and excludes internal notes) and the Invoicing section (invoices use one-click Download PDF, not the browser print dialog). Added `components/formatted-number-input.tsx` to the file-structure tree. Left the dated history entries untouched as a timeline record.
- 2026-06-22: Guarded editing a saved quote against silently reassigning its ID, and corrected the quote-ID sequencing wording. Previously, opening a saved quote in the builder, clearing the Quote ID field, and saving called `next_quote_id` and overwrote the row's `quote_id` with a brand-new number (changing the client-facing reference and burning a daily number). Now `resolveQuoteIdForSave(currentId, quoteDate, existingRowId)` keeps the existing row's `quote_id` when the field is cleared on an edit (both the builder Save-as-draft and the review Prepare paths pass `savedQuoteId`). A custom ID typed by the owner is still used as-is; a brand-new quote still mints a daily number. Also corrected the "server-side / no gaps" framing in `lib/quote-id.ts` and the README sequencing section: the RPC is invoked from the browser with the public anon key (the increment is atomic in Postgres, but it is not an authenticated server action, so anyone with the anon key can burn gaps until auth lands), and a save that fails after the increment can leave a gap (the number is reserved before the row is written). Abandoned pre-save sessions still do not burn a number.
- 2026-06-22: Reconciled the dashboard lifecycle with Accounts Receivable so a $0-contract accepted quote can no longer show "Pending Payments $0.00" on the dashboard while being dropped from AR. Added a single shared definition of "paid in full" — `isPaidInFull(data)` in `lib/invoice-calculations.ts` — which is true only when there is real invoiced money (`totalInvoicedCents > 0`) and nothing is outstanding, keyed on the outstanding balance rather than the per-invoice paid flags. The dashboard `lifecycleStage`, the invoicing page badge, the saved-quote page badge, and the dashboard-card outstanding line all use it, so every surface agrees with the AR table (which already excluded $0 jobs). A $0-contract accepted quote now stays in Client Accepted instead of Pending Payments; a job with a positive paid invoice plus a $0 unpaid invoice now reads as paid in full (nothing is owed), matching AR.
- 2026-06-22: Fixed a data-integrity bug in invoicing where a paid invoice's amount could silently change. Previously, editing the contract amount, rough-in/finish split, or permit fee and clicking Save Changes recomputed every invoice's amount from the new inputs but kept the old `status` ("paid") and `paidAt`. So a paid invoice's amount could be rewritten while it still showed "Paid {date}", which made Accounts Receivable and the dashboard report money that was never actually collected. Now `components/invoice-builder.tsx` resets any previously-paid invoice to unpaid (clearing `paidAt`) when its amount would change on save, forcing the owner to re-mark it paid against the new amount. Before saving, a plain-English warning lists which paid invoice(s) would change and that they will be reset, so it is never a surprise. Re-saving with no changes still keeps paid statuses (amounts match, nothing resets); editing an unpaid invoice's amount is unaffected.
- 2026-06-19: Production hardening pass. (1) Moved quote-ID sequencing server-side. The builder no longer guesses today's next number client-side (a race that could hand two simultaneous saves the same ID); instead a Postgres `next_quote_id()` function atomically increments a per-day counter (`public.quote_sequences`) via `INSERT ... ON CONFLICT ... RETURNING` and returns `Q-YYYYMMDD-NNN`. The number is reserved at save time, not when the builder opens, so abandoned builder sessions do not create gaps in client-facing IDs; the builder's Quote ID field is blank with a placeholder "Assigned on save" until the quote is saved, and a custom ID typed by the owner is still used as-is. New `lib/quote-id.ts` (`resolveQuoteIdForSave`) is shared by the builder's Save-as-draft and the review page's Prepare paths. Owner must run the one-time SQL (counter table + `security definer` function + grant + `quotes.status` default → `draft`) before deploy. (2) Flipped the `quotes.status` column default from legacy `completed` to `draft` (defensive — every app insert sets status explicitly). (3) Deleted the temporary `components/dashboard-build-status.tsx` build tracker and its usage on `/quotes`. RLS tightening is intentionally deferred until owner/admin login exists.
- 2026-06-19: Renamed the quote/invoice PDF entry buttons. The links that lead to the Download PDF pages used to say "Print" (e.g. "Print Detailed Quote", "Print Summary Quote", and a bare "Print" on invoices and the dashboard cards), which implied printing rather than downloading. Now read "Detailed Quote PDF" / "Summary Quote PDF" (saved-quote and review pages), "PDF" (invoice cards and dashboard cards), and the review-page disabled tooltip reads "Save the quote first to download". The URL segments stay `/print` (internal, not user-facing); only the visible labels changed.
- 2026-06-19: Completed the PDF export upgrade across all four customer-facing printables. Each now has one-click Download PDF rendered server-side with `@react-pdf/renderer`. New shared module `components/pdf/pdf-shared.tsx` holds the colors, page style, and header/info-grid/total/notes/footer/list building blocks (copied from the Detailed Quote styles so every PDF looks consistent; the Detailed Quote stays self-contained). New `components/pdf/summary-quote-document.tsx` (condensed category-subtotal summary) and `components/pdf/invoice-document.tsx` (initial/finish invoice with the previously-invoiced sub-box on the finish invoice). New server helpers `lib/summary-quote-pdf.ts` and `lib/invoice-pdf.ts` load the saved snapshot + live settings and build the pre-formatted props, shared by each preview page and its PDF route (`app/quotes/[id]/summary/pdf/route.tsx`, `app/quotes/[id]/invoices/[kind]/pdf/route.tsx`), so the on-screen preview and the downloaded PDF can never drift apart. The Summary and invoice print pages were refactored to render from the shared props and swap the old `window.print()` button for a Download PDF link. Removed the now-dead browser-print components (`print-quote-button`, `invoice-print-button`) and the `.no-print` / `.print-document` / `@page` / `@media print` CSS from `globals.css`. `categoryDisplayName` moved to `lib/calculations.ts` so the Summary preview and PDF agree on category labels.
- 2026-06-19: Fixed red lines/borders in the Detailed Quote PDF. The subtle `rgba(...)` borders and muted text rendered as red/dark artifacts because react-pdf composites alpha-channel colors inconsistently through the PDF graphics state. Replaced every `rgba()` color in `components/pdf/detailed-quote-document.tsx` with a solid hex pre-blended against the page background so it looks the same to the eye but has no alpha channel. Also added the missing `import React`.
- 2026-06-19: Began the PDF export upgrade (pilot on the Detailed Quote). Added `@react-pdf/renderer` and `experimental.serverComponentsExternalPackages: ["@react-pdf/renderer"]` in `next.config.js` (PDFs are rendered on the server, not in the browser). New `components/pdf/detailed-quote-document.tsx` (a react-pdf `<Document>` recreation of the on-screen Detailed Quote: logo + business header, Quote id + date, Prepared For / Project, the line-items table, Quote Total, notes, footer — built-in Times-Bold/Helvetica fonts, brand palette hex). New `lib/detailed-quote-pdf.ts` (server helper that loads the saved snapshot + live settings and builds the plain pre-formatted props the PDF needs; single source of truth shared by the preview page and the PDF route). New `lib/pdf-logo.ts` (server-only; reads `/public/ffe-logo.png` into a base64 data URI so react-pdf embeds the logo with no network fetch). New route handler `app/quotes/[id]/print/pdf/route.tsx` renders the document to a buffer with `renderToBuffer` and streams it back as a file download. New `components/pdf/download-pdf-button.tsx` is now just a plain link to that route (no client-side react-pdf). `app/quotes/[id]/print/page.tsx` uses the shared helper and swaps the old `window.print()` button for the Download PDF link; the existing HTML layout stays on screen as a preview. An earlier client-side attempt (PDFDownloadLink) blanked the page on load because it rendered the PDF eagerly on mount and threw; switched to server-side generation to avoid any client-side react-pdf. Summary Quote and invoices still use the browser print dialog pending the follow-up pass.
- 2026-06-18: Overhauled the dashboard to read saved quotes from Supabase (`dashboard-saved-quotes.tsx`), made it owner-focused with a compact removable build-status panel, removed the old marketing empty state.
- 2026-06-18: Added saved quote route `/quotes/[id]` (server component, loads from Supabase, no localStorage) and wired the dashboard Open buttons to it.
- 2026-06-18: Added owner-only Internal Notes text box in the builder; notes show on owner views (review, saved quote) but never on customer-facing output. No schema change (field already existed).
- 2026-06-18: Enabled editing saved quotes (`/quotes/[id]/edit`) and deleting saved quotes (removes row from Supabase, with confirm). Save now upserts (update vs insert) which also prevents duplicate saves; the Save button locks after success. Added the anon delete RLS policy.
- 2026-06-18: Removed the confusing "Clear Review Quote" button from the review page. After a successful save, the review page now shows "View saved quote" and "Start a new quote" links instead.
- 2026-06-18: Clear the active quote (browser working copy) after a successful save, so the owner is not stuck in a loop with the saved quote still loaded as the active quote. From save onward, the owner works from the saved file via `/quotes/[id]` and `/quotes/[id]/edit`.
- 2026-06-18: Stop quote IDs from duplicating. New quotes now get a daily sequence number (Q-YYYYMMDD-001, -002, ...) by checking Supabase for today's highest number. Editing a saved quote keeps its existing ID.
- 2026-06-19: Added the printable Detailed Quote page (`/quotes/[id]/print`) with FFE branding, contact info, line items, total, and customer-facing notes. Uses the browser print dialog (Print / Save as PDF), no PDF dependency. Enabled Print buttons on the saved quote view and (after save) the review page. Internal notes are excluded (customer-facing only).
- 2026-06-19: Added the quote status pipeline (draft, prepared, accepted). The dashboard is now an async server component that groups saved quotes into three stacked stages (Draft / Prepared / Client Accepted), each with a one-line description of its meaning. New `status-badge`, `quote-status-button`, and `dashboard-quote-section` components; the old self-fetching `dashboard-saved-quotes.tsx` was deleted. The builder gained a "Save as draft" button (status draft, client name required) that saves to Supabase and clears the browser working copy. The review page primary action is now "Prepare" and writes status prepared. The saved-quote page shows a color-coded badge and status-aware actions (Prepare, Mark accepted, Move back to drafts, Reopen as prepared, plus a disabled Start invoicing placeholder). Status changes use `supabase.update` + `router.refresh()`, which requires the anon update RLS policy. Owner must run `update quotes set status = 'prepared' where status = 'completed';` once before deploy so existing saved quotes land in Prepared.
- 2026-06-19: Fixed the dashboard not showing newly saved drafts. Server-side Supabase reads were being cached by the Next.js Data Cache; the shared supabase client now forces `cache: "no-store"` so the dashboard always reflects the latest rows.
- 2026-06-19: Added invoicing for accepted quotes. New `invoice_data` JSONB column on `quotes` (run `alter table public.quotes add column if not exists invoice_data jsonb;` once). New routes `/quotes/[id]/invoices` (setup + invoice list) and `/quotes/[id]/invoices/[kind]/print` (printable invoice). New `lib/invoice-calculations.ts` and components `invoice-builder`, `invoice-paid-button`, `invoice-print-button`, plus an `InvoicePaidBadge`. Initial invoice = rough-in (default 50% of contract, editable) + permit fee; finish invoice = remainder; warn if the split does not total 100%; mark each invoice paid/unpaid; dashboard Accepted card and saved-quote page show outstanding balance. Reuses the printable-document pattern and the client-mutation + router.refresh pattern. Owner must run the alter table SQL before deploy.
- 2026-06-19: Extended the dashboard lifecycle from three stages to five by deriving two new stages from invoice setup. Accepted quotes now split into Client Accepted (no invoices yet), Pending Payments (invoices set up, money outstanding), and Paid in Full (every invoice paid). No schema change and no new buttons; the stages are computed on the fly by `lifecycleStage()` in `lib/invoice-calculations.ts` from the row status plus `invoice_data`. `StatusBadge` now shows the derived stage. The dashboard renders five stacked sections (Stage 1 Draft through Stage 5 Paid in Full), each with its own description and empty state.
- 2026-06-19: Confirmed both one-time SQL migrations are applied in Supabase: zero rows still carry `status = 'completed'`, and the `invoice_data` jsonb column exists. The anon UPDATE policy is also present. Removed the pre-deploy migration reminder from the Pending list.
- 2026-06-19: Added a pending item and standing practice note: only admin may pull a quote back out of the invoicing lifecycle once access levels exist (gate the "Reopen as prepared" / "Move back to drafts" buttons on accepted, pending-payment, and paid-in-full quotes), and the Pending list should be reviewed on every change so accomplished items are removed or marked complete.
- 2026-06-19: Added the printable Summary Quote page (`/quotes/[id]/summary`). Condensed customer-facing companion to the Detailed Quote: one row per pricing category with its subtotal, then the quote total. No unit prices are shown. New `summarizeByCategory` helper in `lib/calculations.ts` groups `clientFacingLines` by category and sums client-facing totals (post pricing-level/contingency multiplier), preserving first-appearance order and dropping zero-total categories. Reuses `PrintQuoteButton` and the `.print-document` browser-print pattern. "Print Summary Quote" links added to the saved-quote page (prepared and accepted branches) and "Summary" links to the prepared and accepted dashboard cards. Marked the "PDF export: Summary Quote next" pending item complete; the only remaining PDF work is the optional react-pdf one-click-download upgrade.
- 2026-06-19: Moved all pricing and customer-facing text out of the static `lib/seed-data.ts` file into Supabase, with an admin UI. New `/pricing-admin` route edits line items, pricing levels, contingencies, project types, and the business-info / default-quote-notes / invoice-payment-terms settings row (deactivate-only, no hard delete; editable sort order). New `lib/pricing.ts` (`getSettings`, `getPricingCatalog`) reads the catalog server-side; the builder pages and the three print pages now read live from Supabase (`force-dynamic`). `calculateQuote` takes the items/levels/contingencies arrays as parameters and resolves level/contingency by stable id with explicit default-id fallbacks. `quote-builder` and `quote-line-item-picker` take the catalog as props; dropdowns show active rows plus the currently-selected value so old quotes still resolve. `lib/seed-data.ts` was deleted. Owner ran the one-time SQL (five tables + seed + anon RLS policies) in Supabase beforehand. Marked the "Pricing admin" pending item complete.
- 2026-06-19: Added the Accounts Receivable view (`/receivables`). Collections page over every accepted quote with invoice setup, split into two stacked tables: Pending Payments (any job with an outstanding balance, including partially-paid) and Historical Paid (fully paid; a job moves here automatically once the last invoice is paid). One row per job with separate rough-in and finish columns (amount + Paid/Unpaid badge + per-invoice outstanding or paid date), plus a job-level total outstanding and the earliest invoice issued date. Preset period filter (All time / This month / Last 30 days / This quarter / This year) and sort (oldest first / largest outstanding / newest / client), applied client-side. New `app/receivables/page.tsx` (server, `force-dynamic`, queries accepted quotes with `invoice_data` not null), `components/receivables-table.tsx` (client filter/sort/partition), `ReceivableInvoice`/`ReceivableJob` types, and small helpers `invoiceOutstandingCents` (`lib/invoice-calculations.ts`) and `formatDate` (`lib/currency.ts`). Reuses `outstandingCents`, `isFullyPaid`, `computeInvoiceAmounts`, `invoiceReference`. Read-only — no new tables or RLS policies. Added a Receivables nav link and a dashboard shortcut. Marked the "Accounts receivable view" pending item complete.
- 2026-06-19: Restructured the app from a single "Dashboard" into a multi-tool suite. `/` is now an overview dashboard (hub): quick actions (Start New Quote, Receivables, Manage Pricing), four summary tiles (active quotes, awaiting payment + outstanding $, paid in full, manage pricing), the unsaved-working-copy resume card, and a 5-item recent quotes list. The quote lifecycle (the five stacked stages) moved to a new `/quotes` route (the quoting tool) with a "Back to dashboard" link and a Start New Quote button. Nav is now Dashboard / Quotes / Receivables / Pricing / Start New Quote. Quote-scoped back-links ("Back to dashboard") became "Back to quotes" → `/quotes`; pricing-admin and receivables keep "Back to dashboard" → `/`. New `app/quotes/page.tsx` (moved lifecycle + `QuotesHeader`); `app/page.tsx` rewritten as the overview. `DashboardResumeActiveQuote` moved to the overview; `DashboardBuildStatus` stays on `/quotes` (still temporary). No data or schema changes.